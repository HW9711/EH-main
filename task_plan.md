# 手柄插拔与报警逻辑修改计划

## 目标
- 报警状态下暂停普通手柄扫描，手柄 EEPROM 校验报警允许另一通道继续识别。
- 非工作状态最后校验通过的插入通道自动成为选中通道。
- 工作状态下非当前通道插拔不影响当前通道运行。
- 当前工作通道拔出后不自动切换到另一通道，等待用户手动确认。
- 上位机能区分 A/B/AB 手柄 EEPROM 校验失败。

## 阶段
- [x] 阶段 1：阅读工程结构和关键链路。
- [x] 阶段 2：加入回归检查。
- [x] 阶段 3：重构 MemoryMsg 到 WorkMessage 装载入口。
- [x] 阶段 4：调整插入、拔出和扫描暂停规则。
- [x] 阶段 5：调整蜂鸣、屏幕和上位机报警区分。
- [x] 阶段 6：运行回归检查和可用构建检查。

## 约束
- 目标文件受 `Agents.md` 约束，新增业务逻辑必须有清晰中文注释。
- 不回退现有未提交改动。
- 不做无关格式化和整文件重写。

## 产品软件接手文档计划

### 目标
- 以主控工程为主线，整理主控、无刷/有刷驱动、步进驱动、外部通信上位机、压力传感器五个工程的软件产品文档。
- 文档要能在不先通读大量源码的情况下，理解架构、状态结构、协议、调试流程、静态风险和测试方法。

### 阶段
- [x] 阶段 1：生成主文档初版，覆盖五工程架构、接口、测试流程和静态风险。
- [x] 阶段 2：复核主控真实 UART 参数、硬件引脚、DMA、软任务周期。
- [x] 阶段 3：补齐 `WorkMessage`、`MemoryMsgA/B`、识别结构、泵结构和控制信号字段说明。
- [x] 阶段 4：补齐事件码、报警码、外控命令矩阵、心跳动态字段、EEPROM 页映射。
- [x] 阶段 5：补齐电机、脚踏、泵、压力闭环、软 UART 和文件接手索引。
- [x] 阶段 6：运行文档静态检查和主控现有回归脚本。
- [x] 阶段 7：根据检查结果做最后修订并交付路径。
- [x] 阶段 8：根据反馈继续深挖算法细节，补齐手柄 EEPROM 认证、识别状态机、页字段解析、专项测试和静态风险。
- [x] 阶段 9：按同等粒度继续深挖电机、脚踏、泵/压力闭环、外控协议和配套下位机工程，补齐算法流程、异常路径和专项测试。
- [x] 阶段 10：按反馈移除文档中的读者称呼表述，补充复杂模块的函数调用流程图和数据流向图。
- [x] 阶段 11：按反馈继续扩展手柄之外复杂模块，为电机、泵/压力闭环、软 UART 压力和外控链路补齐显式函数调用图与数据流向图，并完成静态检查。

### 文档约束
- 主文档路径：`docs/product-software-handoff.md`。
- 本任务只修改文档和进度记录，不改业务源码。
- 中文内容必须保持 UTF-8 可读，避免终端拼接式整文件重写。

## 2026-06-17 电机驱动风险专项分析计划

### 目标
- 详细分析无刷/有刷驱动工程 `D:\EH_main\soft\Temp_save\GE2433_WSYS_2026_4_24\GE2433_WSYS_2026_4_24\0`。
- 详细分析步进工程 `D:\EH_main\reference\shima_waixie\small_2026_3_31\small_2026_3_31`。
- 针对“已发停止命令但无刷/有刷电机仍转”和“步进泵启动速度偶发异常快”核实软件设计风险。

### 阶段
- [x] 阶段 1：确认两个工程结构、入口文件、协议文件和定时/中断输出链路。
- [x] 阶段 2：梳理无刷/有刷串口收包、停止命令解析、PWM/换相/刹车输出关闭链路。
- [x] 阶段 3：梳理步进工程启动命令解析、目标速度装载、加减速参数、定时器输出链路。
- [x] 阶段 4：归纳可导致失停或异常快启动的软件风险、触发条件、断点和验证方法。

### 约束
- 本轮先做证据化风险分析，不修改两个电机工程源码。
- 结论必须绑定到具体文件、函数或代码片段，区分已确认风险和需要实机验证的风险。

## 2026-06-27 主控软件调试报告计划

### 目标
- 生成一份面向测试、临时配置修改和故障定位的 Markdown 调试报告。
- 报告路径暂定为 `docs/software-debug-report.md`。
- 报告不替代 `docs/product-software-handoff.md`，重点放在“哪里改配置、哪里打断点、看哪些变量、按什么顺序定位问题”。

### 阶段
- [x] 阶段 1：扫描主控工程目录、任务入口、模块文件和已有文档，确认报告边界。
- [x] 阶段 2：梳理启动、调度、公共状态、报警和构建配置的调试入口。
- [x] 阶段 3：梳理手柄、屏幕、脚踏、电机、泵、压力、外控、EEPROM 等模块的断点和观察变量。
- [x] 阶段 4：按典型故障现象整理定位流程，避免只按源码目录堆信息。
- [x] 阶段 5：生成 `docs/software-debug-report.md` 并运行文档静态检查。

### 约束
- 本轮只新增或修改文档和计划记录，不改业务源码。
- 中文内容保持 UTF-8 可读。
- 不批量重写旧文档；只新增本轮报告，必要时引用已有接手文档。
- 若发现源码和旧文档不一致，以当前源码为准，并在报告中标明“需实机验证”的位置。

### 错误记录
| 错误 | 触发动作 | 后续处理 |
| --- | --- | --- |
| PowerShell `Get-ChildItem -Filter` 传入多个文件名失败 | 恢复计划文件时误把数组传给 `-Filter` | 后续改为逐文件读取，不重复该命令 |
| `rg` 直接传入 `User\Application\include\pump*.h` 失败 | PowerShell 未按预期展开该通配路径 | 后续改为明确文件路径或只搜目录 |

## 2026-06-27 主控原理版调试报告重写计划

### 目标
- 将 `docs/software-debug-report.md` 从断点清单式报告重写为“设计原理、状态流动、配置入口、故障定位”一体化主报告。
- 合并吸收 `docs/product-software-handoff.md` 的有效内容，但以当前源码为事实依据。
- 保留旧接手文档，只在开头增加新版主报告提示。

### 阶段
- [x] 阶段 1：复核当前源码中启动任务、软任务串行、公共状态、手柄识别、电机、泵、压力、外控和跨工程协议事实。
- [x] 阶段 2：按“为什么这样设计、状态如何流动、代码在哪里、问题怎么查”的顺序重写 `docs/software-debug-report.md`。
- [x] 阶段 3：在旧 `docs/product-software-handoff.md` 开头增加历史资料说明。
- [x] 阶段 4：补充本轮计划、发现和进度记录。
- [x] 阶段 5：运行文档静态检查和关键函数检索。

### 约束
- 本轮只改文档和记录文件，不改业务源码、不改 EIDE/Keil 配置。
- 报告必须能说明核心状态结构的区别、输入输出链路、控制权仲裁、安全停泵和外控超时逻辑。
- 若旧文档和当前源码不一致，新报告写当前源码结论，并单独列为跨工程风险。

## 2026-06-27 产品手册级调试报告 20 轮重写计划

### 目标
- 将 `docs/software-debug-report.md` 重写并扩展为产品手册级主文档，篇幅、细节和调试说明都按最终交付资料处理。
- 文档要覆盖整个主控项目及四个配套工程的交互边界，不能只写断点或目录索引。
- 每轮执行“扩写 -> 自检 -> 记录缺口”，累计 20 轮；自检只写交付视角，不出现学习对象称呼。
- 复杂逻辑必须配文字说明、代码片段和 Mermaid 图；临时配置入口必须给出文件、宏、影响范围和验证方法。

### 20 轮迭代安排
- [x] 第 1 轮：参考开源文档写法，确定本工程主报告结构和验收口径。
- [x] 第 2 轮：扩写工程资料地图、五工程边界、源码目录导航和文档阅读路线。
- [x] 第 3 轮：扩写启动链路、FreeRTOS 任务、软任务串行门控和调试方法。
- [x] 第 4 轮：扩写核心状态模型，加入结构体代码片段、生命周期和状态不变量。
- [x] 第 5 轮：扩写手柄识别、短接检测、EEPROM 认证、RFID 等待和 A/B 记忆装载。
- [x] 第 6 轮：扩写屏幕输入、VP 地址、按键队列、触控运行和 UI 刷新链路。
- [x] 第 7 轮：扩写脚踏输入、校准、行程换算、双踏板切通道和异常定位。
- [x] 第 8 轮：扩写手柄按键、按键映射、运行控制、调速调方向和跟随注水。
- [x] 第 9 轮：扩写控制权仲裁、本地控制、外控控制、释放条件和互斥测试。
- [x] 第 10 轮：扩写电机 UART1 命令、驱动反馈、错误码、失停风险和断点路径。
- [x] 第 11 轮：扩写泵任务、泵类型、A/B 逻辑与物理口、流量限幅和排空逻辑。
- [x] 第 12 轮：扩写压力软串口、CS1237 帧、设备码白名单、闭环降速和保持停泵。
- [x] 第 13 轮：扩写外控协议、授权、命令矩阵、心跳、2 秒停输出和 10 秒释放。
- [x] 第 14 轮：扩写 UI、蜂鸣、LED、报警显示、临时提示和报警码复用风险。
- [x] 第 15 轮：扩写 EEPROM、Flash、Page1/Page2/Page3/Page4/Page6 和参数保存。
- [x] 第 16 轮：扩写跨工程协议，覆盖驱动板、步进板、压力板、上位机协议一致性。
- [x] 第 17 轮：扩写临时配置修改手册，覆盖宏、路径、编译清单、验证命令和回退点。
- [x] 第 18 轮：扩写典型故障排查，每个故障按原理链路、断点、正常值、异常解释组织。
- [x] 第 19 轮：扩写最小测试、专项测试、实机记录模板和交付检查清单。
- [x] 第 20 轮：全篇一致性审查，检查术语、重复、遗漏、代码片段、图表和静态命令。

### 本轮执行范围
- 本轮至少完成第 1 至第 4 轮的实际扩写和自检记录。
- 不修改业务源码、不修改 EIDE/Keil 配置、不做提交。
- 旧 `docs/product-software-handoff.md` 保留为历史资料，主报告继续以 `docs/software-debug-report.md` 为准。

## 2026-06-27 代码段解释增强计划

### 目标
- 在 `docs/software-debug-report.md` 中继续丰富内容，重点解决代码段只有源码、解释不足的问题。
- 每个关键代码段补充“这段代码在解决什么问题、每个分支为什么存在、调试时看什么变量、异常值说明什么”。
- 继续保持产品手册口吻，不精简现有内容，不修改业务源码和工程配置。

### 阶段
- [x] 阶段 1：扫描主报告代码块，找出结构体、启动门控、手柄、输入、电机、泵压力、外控、UI 等解释不足位置。
- [x] 阶段 2：补充通用代码段阅读规则和核心结构体代码解释。
- [x] 阶段 3：补充启动、软任务、手柄识别、屏幕/脚踏/手柄按键代码段解释。
- [x] 阶段 4：补充电机、泵、压力、外控、UI/蜂鸣代码段解释。
- [x] 阶段 5：运行静态检查，更新 `progress.md` 和 `findings.md`。

### 约束
- 本轮只改 `docs/software-debug-report.md`、`task_plan.md`、`findings.md`、`progress.md`。
- 不处理工作区已有业务源码改动。
- 中文必须保持 UTF-8 可读，避免整文件重写。

## 2026-06-27 现场使用增强补充计划

### 目标
- 继续扩展 `docs/software-debug-report.md`，补齐现场定位 SOP、协议帧逐字节判读、报警和保护矩阵、变量字典、配置 cookbook、测试用例表。
- 新增内容必须服务三个使用目标：快速理解原理、快速修改配置、出问题时按步骤定位。
- 保持现有产品手册口吻，不删除既有内容，不修改业务源码和工程配置。

### 阶段
- [x] 阶段 1：补充现场定位 SOP，覆盖上电、手柄、电机、泵压力、外控、构建清单。
- [x] 阶段 2：补充协议帧逐字节判读，覆盖 UART1 电机、UART1 回包、UART5/UART7 泵、CS1237 压力、UART2 外控、UART6 屏幕。
- [x] 阶段 3：补充报警和保护矩阵，覆盖报警码、触发来源、是否停机/停泵、UI/蜂鸣/外控表现、解除条件。
- [x] 阶段 4：补充变量字典，覆盖核心状态、写入者、读取者、正常范围、清零条件和断点。
- [x] 阶段 5：补充配置 cookbook 和测试用例表，按需求写入口、影响、验证、回退和测试记录。
- [x] 阶段 6：运行静态检查并更新 `progress.md`、`findings.md`。

### 约束
- 本轮只改 `docs/software-debug-report.md`、`task_plan.md`、`findings.md`、`progress.md`。
- 不处理工作区已有业务源码改动。
- 新增命令示例不得引入占位符扫描误命中。

## 2026-07-01 主控工程接手理解计划

### 目标
- 在不修改业务源码的前提下，恢复既有调研资料，重新建立当前主控工程的目录、启动链路、核心状态、构建清单和未提交改动认知。
- 为后续优化开发准备可复用的工程地图：先知道入口在哪里、状态如何流动、哪些文件是高风险边界。

### 阶段
- [x] 阶段 1：读取既有 `task_plan.md`、`findings.md`、`progress.md` 和主报告上下文。
- [x] 阶段 2：扫描顶层目录、`User/Application` 业务模块、核心 `.c/.h` 文件清单和当前 `git status`。
- [x] 阶段 3：核对旧模块和旧全局状态名是否重新出现在 EIDE、Keil 和源码清单。
- [x] 阶段 4：阅读启动入口、业务初始化、软任务门控、公共状态结构、通道装载、插拔处理和当前未提交差异。
- [x] 阶段 5：整理给后续开发使用的工程地图和注意事项。

### 约束
- 本轮只做只读调研和计划记录，不改业务源码、不改 EIDE/Keil 构建配置。
- 已存在工作区改动视为用户或历史改动，不回退、不覆盖。
- 中文记录保持 UTF-8，可读性优先。

## 2026-07-01 屏幕偶发点不动软件分析计划

### 目标
- 分析烧录后屏幕正常操作一段时间后偶发按钮无响应的主控软件设计风险。
- 先沿 UART6 输入、屏幕帧解析、按键队列、控制权仲裁、UI 刷新和软任务门控建立证据链，不直接猜补丁。

### 阶段
- [x] 阶段 1：恢复既有调研记录，确认屏幕输入、按键队列和任务门控背景。
- [x] 阶段 2：阅读 `screenkey.c`、`uart6.c`、`sscKEYBH.c`，梳理屏幕触摸帧到业务事件的路径。
- [x] 阶段 3：阅读 `Pubinterface.c` 控制权仲裁和 owner 释放条件，确认屏幕按键被静默丢弃的条件。
- [x] 阶段 4：阅读 `sscUIDP.c`、`bsp_uart.c`、`app_task.c`，确认 UI 刷新和业务回调串行阻塞风险。
- [x] 阶段 5：形成软件设计风险结论和现场验证断点。

### 约束
- 本轮只做只读分析和记录，不改业务源码。
- 结论必须区分源码已确认风险和需要实机验证的触发条件。

## 2026-07-10 业务功能代码简化审查计划

### 目标
- 以已完成大量实机验证的提交 `a39c3b1` 为唯一基线，审查当前主控工程的业务代码结构、重复逻辑、状态所有权和注释可维护性。
- 只提出分阶段优化方案，本轮不修改业务源码，不改变任务调度、独立线程栈、任务周期、硬件映射、通信协议和产品行为。
- 优先处理手柄扫描、泵、脚踏、屏幕、公共状态和外控等维护成本最高的模块，避免新增纯转发层和空包装层。

### 阶段
- [x] 阶段 1：确认 Git 基线、工作区状态和 AC5/EIDE 构建基线。
- [x] 阶段 2：统计业务文件规模、函数规模、任务入口、模块依赖和构建清单真值。
- [x] 阶段 3：并行审查手柄/RFID、泵/脚踏/压力、屏幕/Pubinterface/外控以及电机/报警/公共基础模块。
- [x] 阶段 4：汇总重复代码、职责混杂、全局状态写入、输入边界、死代码和误导注释，并区分重构项与独立缺陷。
- [x] 阶段 5：提出 2 至 3 种优化路线，说明风险、收益、文件边界、验证方式和推荐顺序。
- [x] 阶段 6：向用户提交审查结论和推荐设计，等待确认后再生成实施计划。

### 成功标准
- 每项建议都绑定到当前源码中的具体文件、函数或数据流，不根据目录名称猜测职责。
- 每个阶段都能独立构建、独立实机回归、独立回退；任何阶段不得同时横跨多个高风险安全链路。
- 明确列出不应在纯重构中顺手修改的疑似缺陷，避免行为修复与结构整理混在一起。
- 方案能显著减少 A/B 通道复制、大型函数和跨模块隐式状态写入，同时保留现有对外接口和运行时序。

### 约束
- 本轮只读审查业务源码；仅追加 `task_plan.md`、`findings.md`、`progress.md` 审查记录。
- 不创建分支、不提交 Git、不改 EIDE/Keil 源文件清单。
- 中文结论必须直白说明运行时机、状态影响和保留原时序的原因。
- 历史记录中的重构尝试已被实机证明存在较多 bug，只能作为失败案例；不得复制其代码结构，也不得把其构建通过当作行为正确的证据。
- 任何新方案必须先建立 `a39c3b1` 行为对照，再按单模块、小步、可烧录回退的方式实施。

### 错误记录
| 错误 | 触发动作 | 后续处理 |
| --- | --- | --- |
| `rg` 报告 `Src/userparser.c` 不存在 | 首轮任务入口检索沿用了错误的根目录路径 | 已确认真实路径为 `User/Application/Src/userparser.c`，后续只使用真实路径，不重复该命令 |
| PowerShell 将 include 正则中的 `<` 解释为重定向符 | 使用双引号执行 `rg` include 依赖检索 | 后续改用单引号正则或 `rg -F`，不重复原命令 |
| 状态写入正则把 `==` 条件误识别为赋值 | 首轮用简单 `=` 模式检索跨模块写入 | 该输出只作为人工定位线索，不用于写入者结论；后续使用排除 `==` 的模式并人工复核 |
| PowerShell 把 `stash@{0}` 截成 `stash@` | 直接把 stash 语法放进 `git diff` 参数 | 后续改用 `refs/stash` 解析出的提交，不重复原参数写法 |
| `rg` 检索 `tools` 目录失败 | 历史文档多次引用 `tools/check_*.py`，但当前提交已不存在 `tools` 目录 | 已确认这些脚本不是当前可复现测试资产；后续不再检索该目录 |

## 2026-07-10 业务重构设计固化计划

### 目标
- 将用户确认的“行为锁定、一次一个模块、实机确认后提交”方案写入正式设计规格。
- 本阶段只修改设计和审查记录，不修改业务源码。

### 阶段
- [x] 阶段 1：用户确认推荐设计和提交闸门。
- [x] 阶段 2：生成正式设计规格，写清稳定行为、阶段边界、独立缺陷和验收方式。
- [x] 阶段 3：自检规格中的占位、矛盾、范围和歧义。
- [x] 阶段 4：完成文档静态检查并按工程格式提交设计记录。
- [ ] 阶段 5：请用户复核正式规格，确认后再生成实施计划。

### 约束
- 不修改任何 `.c/.h`、任务周期、构建清单或硬件映射。
- 后续业务代码提交必须等待用户完成实机验证并明确确认。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `git diff --cached --check`报告设计规格末尾多一个空白行 | 首次暂存四份设计和审查文档 | 已删除多余空白行，重新暂存后再次执行完整检查 |

## 2026-07-12 脚踏业务模块简化设计

### 目标
- 在不改变 UART4 协议、25ms 行为周期、脚踏校准值、控制权门禁和安全停机规则的前提下，缩短 `FootControlTask()` 并明确单踏板、双段踏板、双踏板三类流程。
- 优先合并重复的启动、停止、注水泵联动、释放复位和 A/B 通道切换代码，不新增空包装层。

### 阶段
- [x] 阶段 1：确认上一个泵模块版本已实机通过并提交为 `c503979`。
- [x] 阶段 2：读取脚踏任务入口、队列、校准数据和 UI 适配边界。
- [x] 阶段 3：梳理 `FootControlTask()` 三类踏板分支的输入、状态写入和退出条件。
- [x] 阶段 4：给出 2 至 3 种简化方式并确认模块级设计。
- [ ] 阶段 5：设计确认后编写实施计划和行为锁定校验，再开始修改生产代码。

### 约束
- 本设计确认前不修改 `.c/.h` 生产源码。
- 不启用未接入启动链的 `PedalRecvTask_Init()`，不改变独立线程栈和任务调度。
- 每个烧录版本只处理脚踏模块；实机确认前不提交脚踏代码。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| 规格自检误报历史记录中的“占位符检查”说明 | 首次把 `task_plan.md`、`findings.md`、`progress.md` 全部纳入关键词扫描 | 已把占位扫描收紧到本轮新规格，仅检查 `TBD`、`TODO` 和“待定”等实际占位内容 |

## 2026-07-14 当前工程行为保持优化审查

### 目标
- 以当前已实机验证并提交的 `040cd17` 为唯一基线，重新评估剩余业务代码的维护成本和可安全优化空间。
- 所有建议必须保证现有业务功能、任务周期、调用顺序、A/B 映射、协议字段、控制权门禁和安全停机行为不变。
- 已完成的手柄扫描、泵、脚踏、屏幕边界和控制权重构不重复建议，优先寻找尚未治理的高收益位置。

### 阶段
- [x] 阶段 1：恢复历史审查记录并确认当前 Git 基线和干净工作区。
- [x] 阶段 2：统计当前业务文件、函数规模、头文件依赖、全局状态写入和构建清单。
- [x] 阶段 3：复核 Pubinterface、外控、UI、按键、电机、报警和公共基础模块的剩余职责边界。
- [x] 阶段 4：区分纯结构优化、独立缺陷、已完成事项和禁止触碰的隐式时序。
- [x] 阶段 5：按收益、风险和实机回归成本给出推荐顺序，并完成只读交付核验。

### 成功标准
- 每项建议绑定当前源码中的具体文件、函数或数据流，不沿用旧提交的过时统计。
- 推荐项能够单模块实施、独立构建、独立烧录、独立回退，不要求同时修改多个高风险业务链。
- 不修改 `WorkMessage`、`MemoryMsgA/B`、`pumpMessageA/B` 的结构定义；仅允许未来单独阶段修正拼写和内存对齐。
- 本轮不修改任何业务 `.c/.h`、任务调度、协议和工程源文件清单。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| 使用 `rtk type` 读取技能文件失败 | Windows 下 `type` 是 shell 内建命令，不是可直接解析的可执行文件 | 改用 `rtk powershell Get-Content -Raw` 完整读取技能文件，不重复原命令 |
| 首次读取历史规划文件显示中文乱码 | PowerShell 默认输出编码与 UTF-8 文件不一致 | 后续显式设置 UTF-8 输出并确认文件内容本身正常，不做编码重写 |
| PowerShell 文件数组命令报 `Missing argument in parameter list` | RTK 转发时带引号的数组路径被重新解析 | 改用 `Get-ChildItem -Path` 的逗号分隔裸路径 |
| `rg` 拒绝带前后向断言的赋值正则 | 默认正则引擎不支持 look-around | 增加 `--pcre2` 后重新统计 |
| `rg` 在 Windows 路径通配符上报路径不存在 | 直接把 `*.h` 放入目录参数未展开 | 改用目录参数配合 `-g "*.h"` |
| include 正则中的 `|` 被 PowerShell 当成管道 | 双引号命令中的复杂正则被外层 PowerShell 提前解析 | 改用简单文件名检索，不再复用原命令 |
| 空文件检查中的 `$_` 被外层 PowerShell 展开 | 双引号嵌套命令使脚本块变量丢失 | 改用 `Where-Object Length -eq 0` 属性参数形式重新检查 |

## 2026-07-14 手柄物理 A/B 接口统一交换

### 目标
- 新增统一配置 `HANDLE_PHYSICAL_AB_SWAP_ENABLE`，当前启用后让逻辑 A 使用物理 B 手柄资源、逻辑 B 使用物理 A 手柄资源。
- 只交换短接检测、EEPROM 总线、实体运行键、RFID 硬件通道和电机驱动物理通道；逻辑状态、UI、报警、泵映射和 A 后 B 扫描顺序保持不变。

### 阶段
- [x] 阶段 1：核对当前物理资源绑定和全部受影响调用链。
- [x] 阶段 2：先建立映射静态校验，再实施统一配置和最小代码修改。
- [x] 阶段 3：同步 `docs/软件说明手册.md` 的配置入口、硬件映射和维护记录。
- [x] 阶段 4：执行静态校验、EIDE/AC5 全量构建、map 对比和临时文件清理。
- [x] 阶段 5：用户要求将当前工程作为稳定节点提交；提交前重新执行全量构建、配置核对和临时文件清理。

### 成功标准
- `HANDLE_PHYSICAL_AB_SWAP_ENABLE=1U` 时：逻辑 A→物理 B，逻辑 B→物理 A；设为 `0U` 时恢复原映射。
- `MemoryMsgA/B`、`ChannelrecognizeMessageA/B`、`WorkMessage`、屏幕 A/B、泵 A/B 和扫描顺序不变。
- 外控按逻辑通道读写的 EEPROM 与手柄扫描使用同一物理 I2C 总线。
- EIDE/AC5 全量构建 0 error、0 warning；两种宏配置都能编译。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `session-catchup.py` 输出历史上下文时发生 GBK `UnicodeEncodeError` | 脚本打印包含 `↔` 的上下文 | 已从当前对话、Git 状态和规划文件恢复上下文，不重复运行该脚本 |
| 软件说明手册首个大块补丁上下文不匹配 | 维护记录原文与预读摘要不同 | 改为按真实原文分段应用小补丁，既有手册内容未被覆盖 |
| 首次行尾统计命令被嵌套引号错误解析 | PowerShell把正则前向否定片段当成命令 | 改用只读Python字节统计，确认局部补丁产生混合行尾后再以PowerShell机械恢复CRLF |

## 2026-07-15 外控EEPROM安全边界与导航批量读写

### 目标
- 主控协议永久拒绝外部设备写业务EEPROM，只允许读取业务页。
- 导航Page12~Page128支持协议级批量读取和批量写入，并保留现有单页命令兼容性。
- 上位机逐页核对主控回包后再报告完成，任何失败页必须明确显示。

### 阶段
- [x] 阶段1：核对主控单页功能码、整区禁用分支、150字节帧上限和上位机当前逐页循环。
- [x] 阶段2：确定最小批量帧格式并建立失败校验。
- [x] 阶段3：实现主控安全拒写和导航批量处理。
- [x] 阶段4：实现上位机批量命令与逐页结果确认。
- [x] 阶段5：同步唯一软件说明手册并执行主控EIDE及上位机回归验证。

### 成功标准
- 功能码0x07无论权限和状态如何都不能写入业务页，并返回EEPROM失败应答。
- 导航批量请求严格限制Page12~Page128，长度、范围或载荷错误不得访问EEPROM。
- 批量写任一页失败时停止后续写入并返回失败页；批量读逐页上传并有明确结束结果。
- 批量只在手柄电机和A/B泵停止时执行；上位机不得插入单页命令、乱序结算、手工伪造进度或中途清空结果。
- EIDE/AC5全量构建0 error、0 warning；上位机协议构造、解析和安全策略检查全部通过。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| 记忆中的上位机旧路径不存在 | 直接检索`FinalSoft\uart2-external-host-standalone` | 从FinalSoft实际目录重新定位到`外部通信上位机\uart2-external-host-standalone`，未沿用旧路径 |
| 首次Node内联语法检查引号被PowerShell剥离 | 使用`node -e`直接解析HTML脚本 | 改用一次性JS文件完成语法检查，检查通过后立即删除临时文件 |
| 首次空文件枚举中的`$_`被外层命令展开 | 嵌套PowerShell筛选空文件 | 改用精确路径和`rg --files`检查锁文件/临时脚本，未执行任何删除性批处理 |
| Windows路径通配符被`rg`当成非法文件名 | 直接传入`sscPUMP*.c`核对泵状态字段 | 改为检索完整Pump目录和精确源码文件，未修改任何文件 |
| `unify_builder.exe`不在PATH且扩展目录搜索命令无结果 | 直接`where`和带管道的`rg --files`定位构建器 | 从已安装EIDE扩展目录只读定位到3.27.2内置构建器，并先读取`--help`确认`-p`参数 |
| 新增静态回归脚本的函数结束标记不存在 | 用`function initialize`截取`bindParserTools()` | 改用真实下一个函数`bindFaultPopup`，重跑通过后立即删除一次性脚本 |

## 2026-07-15 导航批量读取失败与连接状态抖动

### 目标
- 修复导航批量读取在 Page12 等未初始化或页校验异常页面处立即中止的问题，保留失败页并继续读取后续页面。
- 降低批量上传期间 UART2 连续发送压力，避免页数据、心跳在同一任务周期叠加。
- 可恢复的 Web Serial 读取异常只记录日志，不改变物理连接徽标；真实断开仍按原逻辑处理。
- CRC、帧尾或长度校验失败的心跳不得刷新压力、泵速、实时状态或导出日志。
- 业务 EEPROM 只读、导航批量写遇错即停、外控权限和运动停止门禁全部保持不变。

### 阶段
- [x] 阶段1：读取实机截图和失败原始帧，确认失败对象为实际 Page12、原因为底层设备读取失败。
- [x] 阶段2：核对 AT24CS32 页校验、批量发送节拍、心跳发送和上位机可恢复读异常处理。
- [x] 阶段3：建立失败页继续、批量写遇错即停和连接徽标稳定的回归校验。
- [x] 阶段4：实施主控与上位机最小修改，同步唯一软件说明手册。
- [x] 阶段5：完成上位机语法/协议回归、EIDE全量构建、临时文件清理和交付核验。

### 成功标准
- 批量读遇到某一导航页读取或页校验失败时，返回该失败页并继续后续页，最终状态明确显示“部分页失败”。
- 批量写任一页失败时仍立即终止，绝不继续改写后续页。
- 批量页上传不与同周期心跳叠加，发送节拍留出明确串口裕量。
- 可恢复的 `Framing error` 不再把顶部连接徽标改成异常状态，真实串口断开和心跳超时提示不受影响。
- 无效心跳仍可写入错误日志和协议解析窗口，但不得进入 `applyTelemetry()` 或任何压力/泵速曲线。
- EIDE/AC5全量构建0 error、0 warning；上位机模块脚本语法和批量状态机回归全部通过。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| 两次上位机事件检索正则被 PowerShell 剥离引号 | 组合检索 `addEventListener("status")` 和 error 事件 | 改用单一文字分别检索，错误发生在只读命令解析阶段，未修改文件 |
| 首次合并追加压力证据补丁上下文不匹配 | 同时更新计划、发现和进度三份文件 | 改为按当前真实小节分别应用局部补丁，未覆盖已有记录 |
| Windows PowerShell 5 按 ANSI 读取临时格式脚本 | 脚本内直接写入中文文档和上位机路径 | 改为按工作目录和 ASCII 文件名检查；错误只发生在读取阶段，未写入错误路径 |
| 两次内联 PowerShell 的变量和引号被外层提前展开 | 枚举行尾和构建目录空文件 | 改用无变量的精确路径命令；最终确认构建目录无空文件，未执行递归删除 |
| 最终上位机组合检索的双引号未闭合 | 同一命令同时检索可选链和连接状态调用 | 改为不含嵌套引号的独立文字检索，未修改任何文件 |

## 2026-07-15 EEPROM批量读写进度显示

### 目标
- 在现有EEPROM区域增加批量读写进度条，让使用者能看到当前已处理页数、总页数、百分比和失败页数。
- 进度只由主控逐页成功/失败回包推进，不使用模拟定时动画，不恢复批量期间的整表逐页重绘。
- 全部页面处理后若尚未收到最终ACK，明确显示“正在确认完成”；完成、部分失败、超时和断线均保留真实终态。

### 阶段
- [x] 阶段1：核对现有EEPROM时间统计、批量状态机、逐页结算和最终ACK入口。
- [x] 阶段2：建立进度组件存在性和真实回包驱动的失败校验。
- [x] 阶段3：实现进度条样式、无障碍属性和批量状态更新。
- [x] 阶段4：同步软件说明并完成上位机语法、策略与临时文件检查。

### 成功标准
- 批量开始显示0/总页数；每结算一页后准确更新页数和百分比。
- 失败页计入已处理页数并单独显示失败数量；批量读取可继续，批量写仍按既有规则遇错停止。
- 页面全部返回但最终ACK未到时显示确认状态，不提前宣告完成。
- 进度条具有`role=progressbar`和动态`aria-valuenow/aria-valuemax`，减少动态效果设置下仍可正常使用。
- 不修改主控协议、EEPROM业务边界、串口节拍和固件源码。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| Node和Python内联最终校验被PowerShell剥离引号 | 直接在`-e/-c`参数内嵌HTML属性和正则 | 改用一次性JS文件完成模块语法和进度结构校验，执行通过后立即删除临时文件 |

## 2026-07-15 EEPROM批量活动状态替代进度条

### 目标
- 删除会呈现“0后瞬间满格”的确定型进度条，避免使用者误解批量操作具有均匀可见的百分比进度。
- 改为持续运行状态卡，显示旋转工作指示、实时耗时、已返回页数、最近页面和失败页数。
- 批量结束后把工作指示切换为完成、部分失败或中止终态；主控协议、逐页回包和结果表统一绘制保持不变。

### 阶段
- [x] 阶段1：根据实机反馈确认百分比中间状态未获得浏览器绘制机会，确定改用不定进度活动指示。
- [x] 阶段2：建立新活动状态组件存在且旧进度条清零的失败校验。
- [x] 阶段3：实现活动指示、实时时间和批量终态，并删除旧进度条代码。
- [x] 阶段4：同步说明，完成模块语法、既有回归、临时文件和编码检查。

### 成功标准
- 批量开始后立即出现持续旋转的“处理中”指示，不显示百分比和填充长度。
- 已返回页数、最近页面、失败页数继续取自主控真实回包；实时耗时由本机时钟更新。
- 等待最终ACK时明确显示确认状态；完成、部分失败、超时或断线后停止旋转并保留终态。
- 旧`progressbar`、宽度百分比和相关填充动画全部删除，不影响现有批量读写状态机。
## 2026-07-15 上位机四工作台界面改造

### 目标
- 把当前超长单页改为固定全局状态栏与四个顶层工作台，减少整页上下滚动。
- 视图切换不得重建串口、外控权限、EEPROM批量状态、日志缓存或实时曲线数据。
- 保持现有协议、主控固件、安全门禁、控件ID和业务动作不变。

### 阶段
- [x] 阶段1：核对现有顶栏、三列工作区、控制/监控/EEPROM/诊断面板和状态生命周期。
- [x] 阶段2：新增固定工作台导航并按现有DOM职责重组四个视图。
- [x] 阶段3：实现无重载视图切换、全局安全动作和后台任务状态徽标。
- [x] 阶段4：处理隐藏Canvas重绘、响应式布局、键盘导航和视觉细节。
- [x] 阶段5：完成模块脚本语法、现有回归、浏览器布局和临时文件检查。

### 成功标准
- 1920x1080与1366x768下核心操作不再依赖浏览整张长页面。
- 切换工作台不改变串口连接、外控owner、设备运行、批量任务、日志或实时数据。
- 急停和关键连接状态始终可见；EEPROM任务在其它工作台可看到处理中/完成/失败提示。
- 所有现有上位机回归与模块脚本语法检查通过，主控源码和协议不因本轮界面改造发生变化。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| 复杂正则被PowerShell引号解析拦截 | 一次性检索HTML区块和ID | 改用固定字符串与无变量分段读取；未修改任何文件 |
| PowerShell变量被外层终端提前展开 | 使用变量切片读取HTML | 改用`Select-Object -Skip/-First`无变量读取；未修改任何文件 |
| 临时静态检查首次把JS选择器中的`#id`计为重复DOM ID | 用文本正则直接统计全部`#id` | 收紧为只解析HTML标签ID后通过，未修改业务代码 |
| 临时静态检查首次把CSS选择器计为工作台按钮 | 用宽泛文本模式统计`data-workspace-target` | 改为只解析带该属性的按钮标签后通过，未修改业务代码 |
| Playwright命令行缺少Bash且npm缓存包大小异常 | 通过CLI启动浏览器视觉测试 | 改用已安装Playwright包和本机Edge执行相同页面验证 |

## 2026-07-15 四工作台分栏调节与导出入口恢复

### 目标
- 为实时监控、设备控制、EEPROM维护和通信诊断分别恢复左右栏拖动调宽能力，并记住每个工作台的独立比例。
- 恢复JSONL、通用CSV、压力CSV、手柄CSV和电流CSV等原有独立导出按钮，不再收纳为单个菜单入口。
- 保持单HTML运行时、控件ID、串口连接、外控权限、EEPROM批量状态和协议行为不变。

### 阶段
- [x] 阶段1：恢复上一轮上下文并锁定本轮只处理布局分栏与导出入口。
- [x] 阶段2：核对现有工作台网格、旧分隔器代码和导出按钮绑定。
- [x] 阶段3：实现各工作台独立分栏拖动、键盘调宽和本地比例记忆。
- [x] 阶段4：恢复全部导出按钮并完成中等宽度顶栏适配。
- [x] 阶段5：完成现有回归、浏览器拖动/导出/响应式验证和临时文件清理。

### 成功标准
- 1366×768及以上每个双栏工作台都有清晰可操作的分隔条，拖动只改变当前工作台比例，不影响其它工作台。
- 刷新页面后各工作台恢复上次比例；双击分隔条恢复该工作台默认比例。
- 所有原导出类型均有独立可见按钮，点击继续调用原有处理函数，不复制ID或事件绑定。
- 窄屏纵向布局不显示无效分隔条，现有上位机回归和模块脚本语法全部通过。
## 2026-07-15 上位机工业级视觉体系升级

### 目标
- 参考成熟工业监控产品，重做上位机的表面层级、状态色、通道色和关键数值层级，解决当前界面质感偏平、颜色区分不明显的问题。
- 保留四工作台、每个工作台独立可调分栏、五类独立导出按钮、单一 Web Serial 会话和全部现有控件 ID。
- 本轮只修改上位机视觉与必要的语义状态标记，不改变主控协议、外控权限、EEPROM 安全边界或固件业务代码。

### 阶段
- [x] 阶段1：查阅成熟工业监控产品官方资料并完成当前界面基线截图。
- [x] 阶段2：建立中性深色表面、语义状态色和 A/B 通道色的统一视觉令牌。
- [x] 阶段3：升级顶栏、工作台导航、状态卡、控制卡、表格、日志和批量活动卡的层级与交互反馈。
- [x] 阶段4：为动态状态补充最小语义属性，保证在线、运行、告警、离线同时具有颜色与文字双重提示。
- [x] 阶段5：完成浏览器视觉验收、现有上位机回归、说明文档同步和临时文件清理。

### 成功标准
- 绿色只表达健康或成功，琥珀色只表达等待或警告，红色只表达故障或危险，蓝色表达选中和主操作，青色仅用于实时遥测数据。
- A/B 通道使用不同且稳定的辅助色，同时保留通道文字，不依赖颜色单独传递含义。
- 1366x768 和 1920x1080 下四个工作台无整页滚动；分栏拖动、键盘微调、双击复位和本地记忆正常。
- JSONL、通用 CSV、压力 CSV、手柄 CSV、电流 CSV 五类导出入口和下载行为保持不变。
- 上位机模块脚本语法、唯一 DOM ID、现有回归和浏览器交互检查全部通过；不修改主控固件源码。

## 2026-07-15 上位机前三工作台空间再平衡

### 目标
- 消除实时监控、设备控制和 EEPROM 维护工作台中的大块无效空白，让同屏空间优先承载状态、趋势和操作结果。
- 保持四工作台、可调分栏、顶部单行状态、五类导出、Web Serial 会话和全部业务控件 ID 不变。

### 阶段
- [x] 阶段1：分析用户截图并定位实时监控两栏高度失衡与右侧内部滚动。
- [x] 阶段2：核对前三工作台的 DOM 归属、面板尺寸和当前滚动边界。
- [x] 阶段3：用最小 CSS/DOM 调整压缩空白并保持信息完整。
- [x] 阶段4：在 1366×768、1920×920 下验证前三页无大块空白、无整页滚动和无内容裁切。
- [x] 阶段5：执行既有上位机回归、同步说明文档并清理临时文件。

### 成功标准
- 实时监控左侧状态区与右侧图表区纵向利用率接近，不再出现左栏半屏空白而右栏需要滚动。
- 设备控制右侧状态和趋势完整占满工作区，左侧命令按钮无需上下滚动。
- EEPROM维护页的筛选、操作状态、数据和解释在首屏形成稳定层级，不保留空面板占位。
- 业务控件ID、事件绑定、协议构造、批量状态机、导出格式和分栏持久化保持不变。

### 完成结果
- 实时监控改为左侧状态/运行趋势、右侧全高压力区；1920×920下运行趋势画布274px，1366×768下保留119px，状态区均无溢出。
- 设备控制六项摘要改为单行，趋势画布在1920×920下为245px、1366×768下为93px；图例不再被画布挤出。
- EEPROM右侧数据查看区弹性填充，桌面命令区隐藏2~4px阴影伪滚动；业务表单和批量状态不变。
- A/B设备、刀具和压力卡取消左侧半括号式粗线，改用完整细描边和右上状态点。
- 两种桌面尺寸均为4个工作台、3条分隔线、5个可见导出按钮、0个重复ID、0条浏览器控制台错误。

## 2026-07-16 移除重复自动协议解析窗口

### 目标
- 删除通信诊断页右侧随 TX/RX 自动刷新的独立协议解析面板，避免与左侧通信日志重复展示同一帧。
- 保留通信日志和手动协议解析能力；配置导入区接管释放空间，不留下新的空白区域。

### 阶段
- [x] 阶段1：核对用户截图并确认仅删除自动解析面板，保留日志、手动解析和配置导入。
- [x] 阶段2：梳理自动刷新、日志点击和手动粘贴解析的 DOM 与事件引用。
- [x] 阶段3：删除独立面板和自动刷新入口，把主动解析结果收进手动解析区。
- [x] 阶段4：完成两种桌面尺寸浏览器验收、现有上位机回归和说明文档同步。
- [x] 阶段5：清理临时脚本、服务和锁文件，复核正式差异范围。

### 成功标准
- 通信诊断页不再存在“协议解析窗口”独立面板，实时 TX/RX 不会自动生成第二份协议明细。
- 手动粘贴解析和主动点击日志查看明细仍可用，清空本区能够同时清空输入和主动解析结果。
- 配置导入区填充右栏剩余高度；1366×768、1920×920 下无整页滚动、无明显空白和控制台错误。
- 协议编解码、Web Serial 收发、通信日志、配置导入和五类数据导出行为保持不变。

### 完成结果
- 独立 `parser-panel`、自动 TX/RX 展开调用及其清空状态已删除，实时帧只保留一份通信日志。
- 手动粘贴和点击日志改为在手动解析卡片内按需展开；结果区最大180px并独立滚动，清空本区后重新收起。
- 诊断右栏改为两行，配置导入区弹性填充剩余高度；1366×768和1920×920均无整页滚动、无重复ID和控制台错误。
- 模块脚本语法及5项既有上位机回归通过，说明文档已同步；临时检查脚本、截图和本地HTTP服务已清理。

## 2026-07-16 上位机掉线统计与顶部可读性

### 目标
- 放大顶部链路、外控、当前通道和运行状态文字；1920宽度保持单行，1366宽度分两行保证四项完整显示。
- 上位机继续依据有效心跳记录A/B手柄、泵和RFID识别结果的掉线边沿；RFID丢包率改由主控可配置统计真实读取请求和有效应答后通过心跳上报。

### 阶段
- [x] 阶段1：检查用户截图并锁定顶部可读性与掉线记录范围。
- [x] 阶段2：核对当前心跳字段、刷新周期、串口/心跳超时和 RFID 在线语义。
- [x] 阶段3：定义上位机边沿检测、去抖、会话初始化和重复计数规则，建立失败校验。
- [x] 阶段4：实现掉线统计界面、记录导出/清空、主控RFID统计和顶部字号调整。
- [x] 阶段5：完成A6模拟心跳、双配置构建、浏览器布局、现有回归和说明文档同步。
- [x] 阶段6：清理临时脚本、服务与锁文件，复核正式差异范围。

### 成功标准
- 每个 A/B 设备只在稳定在线后确认掉线时新增一条记录，持续离线不会重复计数；恢复时间和掉线持续时长可追踪。
- 串口刚连接、页面刚打开或首个有效心跳为离线时不制造虚假掉线；心跳无效帧不推进统计状态。
- 上位机串口断开和心跳超时与设备掉线分开显示，不把整条通信链路中断误计为六个设备同时掉线。
- 记录包含设备类型、逻辑通道、掉线时间、恢复时间、持续时长和当前状态，并可清空及导出 CSV。
- 射频丢包率按 A/B 分开统计，以主控实际`Lost / (Valid + Lost)`计算；业务主动取消不计丢失，开关关闭时恢复原心跳并由上位机明确显示等待。
- 顶部状态文字在 1366×768、1920×1080 下使用16px且四项完整显示；1366宽度允许状态和操作分成两行，页面仍不产生整页滚动。

## 2026-07-16 主控RFID超时掉线统计与诊断布局调整

### 目标
- 移除通信诊断页配置导入区，把释放空间用于设备掉线和RFID链路统计，并提高统计字号与层级。
- 主控复用 handlescan 现有“200ms监测、连续10次未确认”掉线判定，按A/B通道累计确认掉线次数并沿用现有单次蜂鸣，不另建重复超时状态机。
- 通过现有A6心跳扩展向上位机实时上报掉线统计，保持当前请求/有效/丢失/异常帧统计兼容。

### 阶段
- [x] 阶段1：核对RFID请求、超时结束、蜂鸣入口、A6字段和诊断页配置导入DOM。
- [x] 阶段2：锁定掉线率分母、重复计数边界和蜂鸣节流规则，建立修改前失败校验。
- [x] 阶段3：实施主控掉线统计、A6协议扩展和上位机解析显示。
- [x] 阶段4：移除配置导入并重排诊断右栏，放大设备健康统计文字，并按Apple/Emil规范完成全局视觉收口。
- [x] 阶段5：完成主控EIDE构建、上位机协议/UI回归、浏览器布局和临时文件清理。

### 成功标准
- 每次连续10轮在线监测未确认且刀具此前在线时只累计一次确认掉线，并沿用既有单次蜂鸣；主动停止、切通道、手动模式或未在线刀具不计掉线。
- A/B通道统计彼此独立；持续离线不会重复累计或蜂鸣，重新识别上线后再次达到阈值才新增一次。
- 上位机实时显示主控请求数、有效应答数、丢失数、异常帧数、超时掉线数和掉线率，手工解析帧不得覆盖现场快照。
- 配置导入DOM、事件绑定和说明全部清除；释放区域由设备掉线统计占用，1366×768与1920×920下文字清晰且无整页滚动。
- 主控统计开关开启/关闭配置均可构建，EIDE/AC5为0 error、0 warning；上位机现有协议与UI回归全部通过。

## 2026-07-16 设备控制页按钮与实时状态可读性优化

### 目标
- 在不改变任何控件ID、事件绑定、协议和控制顺序的前提下，重做设备控制页左侧动作按钮和右侧实时状态卡。
- 按Apple/Emil设计原则建立主操作、停止、通道、方向和工具动作层级，并让手柄/泵图标在离线、在线、工作和报警状态下清晰可辨。

### 阶段
- [x] 阶段1：检查用户截图，锁定按钮层级混乱、图标过小和状态对比不足的问题。
- [x] 阶段2：核对设备控制页DOM、按钮ID、状态属性和现有响应式边界。
- [x] 阶段3：实施设备控制页专用CSS和必要的非业务语义标记。
- [x] 阶段4：执行浅色/深色、1366×768、1920×920浏览器视觉与交互回归。
- [x] 阶段5：同步说明文档，执行语法/编码/临时文件检查并收口。

### 成功标准
- 左侧按钮按动作语义形成稳定层级，启停成对且停止操作明显但不过度报警化；按钮不再呈现默认表单控件质感。
- A/B手柄和A/B泵状态图标尺寸、描边与文字对比明显提升，在线/离线/运行/报警同时具有颜色、文字和状态点提示。
- 1366×768无整页滚动，左侧命令区不裁切关键动作；暗色、浅色和减少动态效果均正常。
- 页面无重复ID、无控制台错误，Web Serial、协议构造、控制按钮事件与五类导出保持不变。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| PowerShell嵌套命令中的`$paths/$p`被外层提前展开 | 批量统计三个设计技能文件行数 | 改用`rtk proxy rg -n`分文件读取，并按行段补齐完整技能内容；未修改工程文件。 |
| RTK默认检索只展示前25条命中 | 尝试一次输出完整设计技能 | 改用`rtk proxy`关闭摘要过滤并分段读取，避免重复原命令。 |
| 浏览器整页和裁剪截图均超过CDP截图时限 | 尝试生成设备控制页视觉截图 | 改用浏览器真实DOM尺寸、主题变量、语义色和控制台日志逐项校验；未把截图失败误报为视觉验证通过。 |
| 第一版语义色选择器优先级低于控制按钮基础规则 | 极昼主题检查发现启停按钮仍继承主色 | 给语义选择器补齐命令区、分组和button限定，使启动、停止、A/B、刀具色在三套主题中真实生效。 |
| Node REPL 文件系统桥接给6个文本文件增加4096字节异常前缀 | 尝试机械统一本轮触碰文件的CRLF行尾 | 立即停止该写入路径，从隔离视图按原始长度和SHA256恢复完整正文；随后用Windows真实文件系统复核UTF-8、CRLF、文本头和浏览器加载，未丢失代码或文档内容。 |

## 2026-07-16 上位机四工作台切换流畅度优化

### 目标
- 定位四个工作台切换时的同步布局、图表重绘和大面积视觉特效开销，并以最小修改降低点击后的可感知延迟。
- 保持Web Serial连接、协议解析、外控状态、EEPROM任务、统计数据、控件ID和工作台内容完全不变。

### 阶段
- [x] 阶段1：核对切页函数、工作台CSS显隐规则、Canvas刷新入口和现有动画。
- [x] 阶段2：建立实际浏览器切换耗时基线并锁定主要长任务。
- [x] 阶段3：实施最小性能修复和短时、可中断的视觉反馈。
- [x] 阶段4：完成四页连续切换、三套主题、减少动态效果和现有上位机回归验证。

### 成功标准
- 点击或键盘切换后，活动标签在当前帧更新；页面内容不出现长时间空白、闪烁或被旧页重绘阻塞。
- 切页不重复执行隐藏工作台的Canvas重绘和无关DOM更新；动画只使用`opacity/transform`且不超过160ms。
- 快速连续切换可被立即打断，最终工作台、ARIA状态和现有数据保持一致。
- 内嵌脚本语法、UI策略及现有上位机回归通过；无临时脚本、截图、锁文件或空文件残留。

## 2026-07-17 外控切换手柄通道成功提示音

### 目标
- 外控已经取得控制权且真实完成 A/B 通道切换后，蜂鸣器提示一次。
- 重复选择当前通道、切换失败、方向/模式/刀具切换均不产生该提示音。
- 报警蜂鸣进行中不得被外控通道提示音清除或打断。

### 阶段
- [x] 阶段1：核对上位机下行命令、主控通道切换分支、本地屏幕蜂鸣入口和蜂鸣任务优先级。

## 2026-07-24 固定方向手柄与MXYTP机械往复适配

### 目标
- TMBA、TMBB、EMBA、EMBB、JMB、MXYTM只允许EEPROM默认方向；当前方向高亮，其它方向显示灰色禁用态，所有本地/外控换向入口均不能切换。
- MXYTP屏幕高亮往复，正转和反转显示灰色禁用态；电机实际始终按正转下发，避免机械往复结构收到电气往复命令。

### 阶段
- [x] 阶段1：核对七类手柄枚举、Page4默认方向装载、方向显示状态、换向入口和电机下发链。
- [x] 阶段2：确定最小能力判定入口和MXYTP显示/执行方向分离规则。
- [x] 阶段3：实施局部代码修改并建立静态方向矩阵检查。
- [x] 阶段4：使用EIDE实际参数全量构建并清理临时文件。

### 成功标准
- 六类固定方向手柄上电/切通道后高亮EEPROM默认方向，其它方向保持灰色可见但不可选；屏幕、手柄键和外控命令均不能改变该方向。
- MXYTP屏幕高亮往复、正反转置灰，驱动命令中的实际方向固定为正转；其它手柄方向行为不变。
- RFID标签值按0.1A换算到驱动0.01A字段，标签100必须形成10.00A阈值；EIDE/AC5全量构建为0 error、0 warning。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `apply_patch`无法读取工程根文件 | 追加本轮计划 | Windows分离可写根沙箱在读取前拒绝且未修改文件；改用C:\tmp补丁文件，经`git apply --check`后应用。 |
| 两次内层PowerShell变量或引号被外层展开 | 分段读取源码 | 命令均在只读阶段失败且未改文件；改用`rg`精确查看。 |
| 首版两个临时补丁hunk计数或上下文不匹配 | `git apply --check` | Git在写入前拒绝，未形成部分修改；修正后再次校验并应用。 |
| 首次全量构建只给1秒工具超时 | EIDE/AC5 `--rebuild` | 构建进程在编译初期被终止；立即用120秒超时从头全量重建并通过。 |
| 沙箱内无法删除C:\tmp补丁 | 清理一次性补丁 | 精确列出全部已发现路径并申请受控权限删除，最终复核无残留。 |
- [x] 阶段2：建立结构校验并实现空闲时提示音消息。
- [x] 阶段3：接入外控真实通道变化成功边沿并同步软件说明手册。
- [x] 阶段4：执行静态回归、EIDE/AC5 全量构建和临时文件清理。

### 成功标准
- A→B、B→A 成功时各产生一次 100ms 提示音；A→A、B→B 不响。
- 离线通道、无外控权、运行中或报警中的失败请求不响，原失败 ACK 不变。
- 持续报警和限时报警期间忽略此次提示音，不清除报警码或内部倒计时。
- EIDE/AC5 全量构建为 0 error、0 warning，工作区无临时脚本、锁文件和空依赖文件。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `session-catchup.py` 在输出包含 Unicode 箭头时触发 GBK `UnicodeEncodeError` | 恢复本轮文件化计划上下文 | 直接读取现有计划、发现和进度末尾完成恢复；未修改业务文件，也未把脚本异常当成工程问题。 |
| 首次追加计划补丁的末尾锚点不在目标文件 | 同时追加三份规划记录 | 补丁整体未应用；改用各文件当前最后一行作为独立锚点，不影响业务源码。 |
| RED 校验中的 PowerShell 变量被外层提前展开 | 检查专用蜂鸣入口是否不存在 | 命令在文件读取前解析失败且未修改文件；改用直接 `rg` 和最终结构检查验证，不重复原命令。 |

## 2026-07-17 外控切换通道时脚踏图标短暂变黄核实

### 目标
- 追踪外控 A/B 通道切换期间脚踏图标由白色短暂变黄的真实状态链和刷新顺序。
- 区分运行控制权是否真的短暂切到脚踏，与屏幕仅显示了通道记忆中的脚控模式。
- 本轮只读核实，不修改主控、屏幕或上位机代码。

### 阶段
- [x] 阶段1：核对外控切换通道前后 `WorkMessage.drivetype_work` 与外控 owner 的变化。
- [x] 阶段2：核对通道记忆装载、运行快照刷新和脚踏图标颜色判断的调用顺序。
- [x] 阶段3：确认后续哪一任务把图标恢复为白色，并给出最小修复边界。

### 成功标准
- 给出造成黄色瞬态的确切字段、写入函数、显示函数和恢复来源。
- 明确该瞬态是否会让脚踏获得实际控制权或触发电机/泵动作。
- 工作区除既有未提交修改及规划记录外不产生业务源码差异。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| Windows 路径通配符 `User/Application/include/*.h` 被 `rg` 当成非法文件名 | 同时检索控制模式显示接口和头文件 | 已从精确源码结果取得关键调用，后续改用具体文件或目录参数；未修改任何业务文件。 |

### 确认后的实施
- [x] 在 `Pubinterface_LoadChannelMemory()` 中把外控模式优先级放到脚踏在线优先级之前。
- [x] 静态确认外控分支不写 `MemoryMsgA/B.drive_type`，非外控脚踏优先分支保持原样。
- [x] 使用 EIDE 实际 `builder.params` 完成 AC5 全量重建，结果为 0 error、0 warning。


## 2026-07-20 RFID重试、泵队列与压力数据校验修复

### 目标
- 恢复 RFID 普通3次、快速10次的有限重试，成功、业务取消和自然耗尽语义保持不变。
- A/B泵队列满时不提前记住失败消息，调用方下一周期可继续提交最新设定。
- CRC正确的压力帧还必须通过24位原始值、重量和阈值合理性检查；异常后连续3帧合理数据才恢复写入业务状态。

### 阶段
- [x] 阶段1：恢复计划记录并确认三项根因和当前未提交文件范围。
- [x] 阶段2：建立三项修改前失败校验。
- [x] 阶段3：分别实施RFID、泵队列和压力业务校验最小修改。
- [x] 阶段4：执行针对性静态回归和EIDE/AC5全量构建。
- [x] 阶段5：清理临时文件并复核工作区只增加本轮正式源码差异。

### 成功标准
- RFID普通请求最多发送3次、快速请求最多发送10次，下一周期进入原自然耗尽分支。
- 队列发送失败时不更新去重缓存；相同最新泵设定可在队列腾出空间后成功重试。
- 超出24位ADC范围、WeightX10超过50000g或ThresholdG超过50000g的帧不得刷新在线时间、压力闭环或外控数据；异常后第3个连续合理帧恢复。
- EIDE/AC5全量构建0 error、0 warning；不修改任务周期、消息结构、压力板工程和现有用户文件。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| apply_patch无法准备Windows分割可写根目录沙箱 | 追加计划记录 | 补丁在读取文件前被拒绝，未改文件；改用严格UTF-8唯一锚点替换并逐项校验。 |
| git apply -没有收到内存补丁 | 尝试不落盘应用补丁 | 命令直接返回无有效补丁，未改文件；随后使用执行完即删除的临时补丁。 |
| 压力文件首个补丁上下文与当前源码注释不一致 | 应用压力业务校验补丁 | 补丁整体未应用；重新读取真实上下文后用5个唯一锚点完成局部替换。 |
## 2026-07-20 剩余可靠性与维护性改进方案

### 目标
- 以提交`667bf66`为基线，排除已完成的RFID重试、泵队列重试和压力数据合理性校验，只评估剩余改进项。
- 按嵌入式安全、实时性、通信健壮性、资源余量、可诊断性和维护成本排序。
- 每项方案保持现有业务协议、任务周期、公共数据结构和控制时序不变，能够独立构建、回归和回退。

### 阶段
- [x] 阶段1：恢复既有审查记录并确认当前Git基线。
- [x] 阶段2：复核当前风险章节和真实源码入口，删除已经修复或已经失效的旧结论。
- [x] 阶段3：为剩余问题定义最小修改边界、验证方式和实施优先级。
- [x] 阶段4：输出分阶段实施方案，明确暂缓项和业务行为不变的验收门槛。

### 成功标准
- 每个建议都有当前源码证据、影响场景、修改范围、回归方法和回退边界。
- 不把潜在行为缺陷伪装成纯重构；需要产品决策或实机阈值的项目单独列出。
- 本轮不修改业务源码、不改变配置宏、不生成固件。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `session-catchup.py`输出历史中文时再次出现终端乱码 | 恢复上一会话未同步上下文 | 已直接读取UTF-8计划、发现和进度文件，并用`git status`核对当前磁盘真值；乱码仅影响脚本显示。 |
## 2026-07-21 Page3减速比与增速比两位小数适配

### 目标
- 让手柄EEPROM Page3中的减速比和增速比支持两位小数，并保持主控、EEPROM写入工程和外部通信上位机解释一致。
- 优先保持Page3现有32字节布局、页尾校验和Page1认证输入不变，避免扩大EEPROM数据面或改变业务页外控安全边界。
- 明确旧EEPROM整数数据的兼容规则，防止升级固件后历史手柄倍率被错误放大或缩小。

### 阶段
- [x] 阶段1：追踪三工程的Page3字段定义、端序、输入校验、显示和运行计算。
- [x] 阶段2：确定两位小数定点编码、取值范围和旧数据兼容策略。
- [x] 阶段3：分别实施主控、EEPROM写入工具和外部通信上位机最小修改，并同步唯一软件说明手册。
- [x] 阶段4：补齐或更新编解码回归，执行上位机静态检查和两套固件全量构建。
- [x] 阶段5：删除一次性校验产物，复核三工程Git状态、空文件、锁文件和最终差异。

### 成功标准
- 单独配置减速比`5.25`或增速比`2.75`时，EEPROM写入值、读回显示和主控运行计算保持一致，最多保留两位小数。
- 现有整数配置仍能得到原倍率；Page3页尾和Page1认证继续按原规则通过。
- 外控业务EEPROM保持只读，不因本次显示适配开放业务页写权限。
- 主控与EEPROM写入板均全量构建为0 error、0 warning；正式测试通过且无临时脚本、锁文件或空依赖残留。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| PowerShell分段读取命令中的变量被外层提前展开 | 恢复MEMORY与规划记录上下文 | 命令在读取前解析失败且未修改文件；改为`Select-Object -Skip/-First`无变量读取并继续。 |
| `apply_patch`无法准备Windows分割可写根目录沙箱 | 追加本轮计划记录 | 补丁在读取文件前被拒绝且未修改文件；改用经`git apply --check`验证的UTF-8最小补丁。 |
| 外部上位机目录不是Git仓库 | 并行读取三工程Git状态 | 只读`git status`返回非仓库；后续把该目录作为独立文件目录核对，不重复执行Git操作。 |
| PowerShell行数读取脚本的引号被外层解释器剥离 | 取得规划文件末尾锚点 | 命令在读取前失败且未修改文件；改用UTF-16LE `EncodedCommand`读取。 |
| 沙箱声明的`C:\tmp`拒绝创建一次性补丁 | 应用规划文件增量 | 写入在文件创建前失败，规划与业务文件均未改变；改用当前工程根目录临时补丁并在应用后立即删除。 |
| 首个带UTF-8末行上下文的Git补丁未匹配工作树 | 追加三份规划记录 | `git apply --check`失败且未修改文件；改用按当前精确行数定位的`--unidiff-zero`纯追加补丁。 |
| 独立复跑单元测试时系统临时目录被沙箱拒绝写入 | 验证EEPROM写入工具71项测试 | 首次运行仅出现`PermissionError`且无断言失败；经受控权限在系统临时目录重跑后71项全部通过，并清理测试残留目录。 |

## 2026-07-23 UART2简易外控协议并行支持

### 目标
- 在现有UART2外控协议之外新增独立的6字节简易协议：`AA BB CC FunCode EE FF`，支持用户给出的`0x01～0x0C`控制功能。
- 新协议的解析、状态和命令分发放在独立业务模块，不把功能码分支混入现有协议状态机。
- 增加独立编译开关；关闭时新协议接收与执行路径不生效，现有外控协议行为保持不变。

### 阶段
- [x] 阶段1：核对UART2接收链、现有外控owner、安全门禁及12项业务动作的真实入口。
- [x] 阶段2：确定简易协议与现有协议的共存、登录、超时、无回包及异常帧规则。
- [x] 阶段3：新增独立协议模块，以最小入口接入UART2，并同步四套EIDE/Keil源文件清单。
- [x] 阶段4：建立协议字节流与功能码回归，分别构建宏开启/关闭配置。
- [x] 阶段5：同步唯一软件说明手册，清理临时文件并复核最终差异。

### 成功标准
- 新协议仅接受完整固定6字节帧，支持粘包、半帧和噪声后的重新同步；错误头尾或未知功能码不得刷新外控链路或执行动作。
- `0x01`通过现有仲裁取得外控权，`0x02`安全停止并退出；`0x03～0x0C`只有在新协议已经登录且现有安全条件允许时才执行。
- 新协议与旧协议不能同时绕过owner；任一协议的停机、退出和链路保护均复用现有安全输出入口。
- 编译宏关闭时新协议功能不可用；宏开启/关闭均通过EIDE/AC5全量构建，结果为0 error、0 warning。

### 协议边界结论
- 用户只定义了下行6字节指令，没有定义响应帧；实现不创造新ACK，并在静默复用原业务分发时抑制旧协议ACK，原100ms心跳保持不变。
- 调速复用当前Page6小步进；泵流量复用既有0～5档类型表；`0x0C`只关闭蜂鸣和弹窗，不清安全报警锁存。

### 错误记录
| 错误 | 触发动作 | 处理结果 |
| --- | --- | --- |
| `apply_patch`无法准备Windows分离写入根沙箱 | 追加本轮实施计划 | 补丁在读取前被拒绝且未修改文件；第一次回退错误写入转义字符串并扰动行尾，已按唯一字节标记精确截断恢复；随后改用UTF-8字节追加。 |
| 临时参数文件没有覆盖板级宏 | 首次验证协议关闭构建 | 构建虽然通过但尺寸与启用版相同，因此不计为关闭验证；随后短暂把真实宏改为0U完成全量构建，立即恢复1U并删除临时脚本。 |
| `unify_builder`缺少`-p`参数 | 最终默认启用版重建 | 命令在读取参数前退出且未开始编译；按当前版本帮助改为`-p builder.params`后全量构建通过。 |
