# EIDE Local Rules

本文件作用于 `EIDE/` 目录及其全部子目录。

## 注释与编码要求

- 以后在本工程中新增或修改代码时，默认必须补充详细、专业、可维护的中文注释。
- 注释应说明模块职责、关键状态机、时序假设、硬件约束、边界条件和异常处理，避免只写“变量赋值”这类低价值注释。
- 新增注释、文档和新建源码文件时，必须确保中文不乱码；优先使用 UTF-8 编码。
- 遇到历史文件存在 GBK/ANSI/乱码风险时，除非任务明确要求整体转码，否则应尽量做最小改动，避免因整文件重写引入新的编码损坏。
- 若某个文件已经存在中文乱码，新增代码时应优先在局部使用确认无乱码的中文注释，必要时以英文或 ASCII 过渡，不能继续制造新的乱码内容。
