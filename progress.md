# 手柄插拔与报警逻辑进度

## 2026-05-26
- 已读取工程内 `Agents.md`。
- 已定位屏幕按键、扫描、通道切换、蜂鸣和上位机报警链路。
- 开始执行最终方案，先加入回归检查再改业务代码。
- 已加入 `tools/check_handle_channel_logic.py`，首次运行失败在 `HandleSwitchActive()` 的 B 切 A 分支，符合预期红灯。
- 已完成 `Pubinterface_LoadChannelMemory()` 公共装载入口，屏幕/上位机/插拔自动选择都复用 MemoryMsg 通道记忆。
- 已调整插入策略：非运行状态最后插入且校验通过的通道自动选中；运行中另一路插入只更新对应 MemoryMsg。
- 已调整拔出策略：非当前通道拔出不影响当前通道；当前通道拔出不自动切到另一通道，等待用户手动确认。
- 已新增 A/B/AB 手柄 EEPROM 校验报警码，并让上位机 `ExternalCommHost.html` 区分显示。
- 已新增 3 秒限时蜂鸣、屏幕临时提示和上位机临时报警帧，用于运行中另一路坏手柄提示。
- 已运行 `python tools\check_handle_channel_logic.py`，8 项检查通过。
- 已运行 EIDE `unify_builder.exe --rebuild --no-color`，构建通过并生成 `build/MainCtrlF413MXOs/MainCtrlF413MXOs.hex`。
- 根据现场测试补充修正：非工作状态下当前选中通道拔出时，如果另一通道仍在线，则自动回落选中另一通道；运行中当前通道拔出仍保持不自动切换。
- 已再次运行 `python tools\check_handle_channel_logic.py`，8 项检查通过。
- 已再次运行 EIDE `unify_builder.exe --rebuild --no-color`，构建通过并生成新的 `build/MainCtrlF413MXOs/MainCtrlF413MXOs.hex`。

## 2026-05-27
- 已新增 `docs/product-software-handoff.md`，以主控工程为主线整理 5 个工程的软件架构、接口协议、测试调试流程和逐文件静态风险。
- 已运行 `python tools\check_handle_channel_logic.py`，8 项手柄通道逻辑检查通过。
- 已运行文档占位符/乱码扫描，未发现常见占位标记或 Unicode 替换符；已用 UTF-8 读取前 20 行确认中文可读。
- 创建 `docs` 目录时旧版 PowerShell 不支持 `New-Item -LiteralPath`，已改用 `New-Item -Path` 完成目录创建。
- 继续扩展 `docs/product-software-handoff.md`：补入主控硬件引脚/DMA 表、公共结构体字段表、事件码表、电机命令/回包/错误映射、脚踏协议、泵方向与压力闭环、外控命令矩阵和动态心跳、EEPROM 页映射、压力软串口解析细节、主控文件接手索引。
- 已修正文档中 `Pubinterface.c` 的历史错误路径，统一为 `User\Application\Pubinterface\Pubinterface.c`。
- 已在 `task_plan.md` 和 `findings.md` 追加产品软件接手文档任务记录，和旧手柄插拔任务区分。
- 已再次运行 `python tools\check_handle_channel_logic.py`，8 项检查通过。
- 已再次运行文档占位符、乱码、旧错误路径扫描，未发现命中；已用 UTF-8 读取文档开头确认中文可读。
- 当前文档约 1147 行，覆盖主控启动、调度、硬件、公共状态、事件码、报警、电机、脚踏、泵、压力、外控、EEPROM、四个配套工程、测试调试和逐文件风险。
- 根据用户反馈继续深挖文档细节：已复核 `handlescan.c`、`at24cs32_crc_verify.c`、`at24cs32.c`、`Pubinterface.c` 中手柄 EEPROM 认证、页校验、状态机、Page2/3/4 解析、A/B 通道装载和速度/频率调节逻辑。
- 已在 `docs/product-software-handoff.md` 的 4.3 节新增“手柄 EEPROM 认证与识别算法”，补入状态机表、认证伪代码、页校验公式、CRC 多项式、Page2/Page3 映射、Page4 字段解析、识别结果流向图和调试步骤。
- 已在第 7.3 节新增手柄 EEPROM 算法专项测试用例，覆盖 Page1 页校验、Page2~8 认证数据、SN 读取、Page2/Page3 未知型号、Page4 速度钳位、默认方向和运行中另一路坏手柄。
- 已在第 8 章和第 12 章补充静态风险：B 通道速度上下限疑似串用 A 通道、B 通道减频判断疑似错误、Page2/Page3 未知型号可能不持续报警、Page4 默认方向字段当前不生效。
- 已运行 `python tools\check_handle_channel_logic.py`，8 项手柄通道逻辑检查通过。
- 已运行文档占位符、乱码和旧路径扫描，未发现异常命中；已用 UTF-8 读取文档开头确认中文可读。
- 根据用户反馈继续把手柄之外模块补到同等粒度：已复核 `sscDrive.c`、`motoruartdata.c`、`sscFOOT.c`、`pedal.c`、`sscPUMPA.c`、`sscPUMPB.c`、`pump_pressure_control.c`、`soft_uart.c`、`external_comm_protocol.c`、`external_comm_task.c`，以及无刷/有刷驱动、步进驱动、压力传感器和外部通信上位机工程的关键文件。
- 已在 `docs/product-software-handoff.md` 第 4 章新增或深化：脚踏解析/定标/行程控制算法、电机命令生成和驱动回包处理算法、泵输出与压力闭环算法、软 UART 压力帧解析算法、外控帧解析/授权/保活/安全动作。
- 已修正文档中电机主控下发帧描述：当前主控下发 11 字节 `AA ... BB AA`，驱动接收端以 `RxCRC == 0xAABB` 兼容；驱动回包才是 12 字节 CRC 小端。
- 已深化第 5 章配套工程：补充驱动板私有协议接收、驱动回包字段、步进/泵板 CRC 旁路与开环算法、压力传感器主循环/标定表/霍尔设备码算法。
- 已新增第 7.4 脚踏专项测试，并扩展电机、泵压力、外控测试用例，覆盖定标异常、短包噪声、回包 12B 边界、压力线性降速/硬停/恢复、软 UART 同相碰撞、外控半包/粘包/急停/EEPROM 长度。
- 已扩展第 8 章和第 12 章风险：脚踏除 0、JTB 长度保护、JTD 右踏在线判断、电机命令依赖 `0xAABB` 旁路、驱动回包边界、A/B 排空周期差异、压力设备码差异等。
- 已运行 `python tools\check_handle_channel_logic.py`，8 项手柄通道逻辑检查通过。
- 已运行文档占位符、乱码、旧错误路径扫描，未发现命中；已用 UTF-8 读取文档开头确认中文可读。
- 当前文档约 1643 行，阶段 9 已完成。
- 根据用户反馈修订文档表述：已将主文档中的读者称呼类字样替换为“接手阅读顺序”“接手提示”“接手时先看什么”等正式产品文档口径，并同步调整 `task_plan.md` 目标描述。
- 已在手柄 EEPROM 认证与识别章节补充 3 张 Mermaid 图：手柄识别函数调用流程图、手柄识别数据流向图、手柄运行按键调用与控制数据流图。
- 已运行读者称呼类字样扫描，未发现命中。
- 根据用户进一步反馈，已把“手柄同等深度”的要求扩展到其它复杂模块，而不是只补手柄相关内容。
- 已在 `docs/product-software-handoff.md` 补充电机函数调用流程图和电机数据流向图，覆盖 `SscDriveMotorTask_Init()`、`MOTORRUNTask()`、`MOTORRUN()`、`MotorStart()/MotorStops()`、`MotorUartData_Init()`、`MOTORUARTTaskFunc()`、`BrushlessMotorUartData_ReceiveData()` 和驱动回包报警链路。
- 已补充泵函数调用流程图和泵/压力数据流向图，覆盖 A/B 泵统一 25ms 周期、队列兼容入口、`pumpMessageA/B`、新启动沿解除压力锁止、UART5/UART7 物理口映射和外控心跳字段来源。
- 已补充软 UART 压力函数调用流程图和压力帧数据流向图，覆盖 `SimUartTask_Init()`、PE4/TIM11 与 PE6/TIM13 独立位采样、双通道接收状态、环形缓冲、CS1237 帧校验和 `pumpMessageA/B` 压力字段写入。
- 已补充外控函数调用流程图和外控数据流向图，覆盖 UART2 DMA、RX FIFO 半包/粘包重同步、协议解析、授权/owner、业务分发、链路看门狗、动态心跳和 EEPROM 读写数据走向。
- 已运行读者称呼类字样扫描，未发现命中。
- 已运行文档占位符、乱码标记、历史路径和常见错字扫描，未发现命中。
- 已运行 `python tools\check_handle_channel_logic.py`，8 项手柄通道逻辑检查通过。
- 已扫描主文档图表标题，当前复杂模块图覆盖手柄、脚踏、电机、泵/压力、软 UART 压力和外控链路。

## 2026-06-17 电机驱动风险专项分析
- 已开始分析无刷/有刷驱动工程和步进工程，目标是定位“停止命令后仍转”和“步进泵启动速度偶发异常快”的软件设计风险。
- 已确认两个目标路径存在；本轮先只读源码和记录风险，不改电机工程源码。
- 已完成无刷/有刷停止链路第一轮证据记录：停止命令进入后走刹车状态机，不是收包处直接硬停；已记录 CRC 旁路、刹车阈值依赖和 B 通道状态变量风险。
- 已补充有刷停止链路证据：串口停止只让 `Start=0/u32SetSpd=0`，实际关 PWM 依赖有刷状态机刹车计时和 Buck PWM 递减；同时记录了 A/B 共用 Buck PWM 变量的交叉影响风险。
- 已完成步进启动速度链路第一轮证据记录：UART3 IDLE DMA 长度未参与解析、`BB AA` 旁路、速度未钳位、`MCPara[25]` 影响启动斜坡，均已写入 `findings.md`。

## 2026-06-27 主控软件调试报告
- 已收到新目标：生成一份非常详细的软件调试报告，便于理解软件细节、快速定位临时配置位置、按故障现象打断点定位 bug。
- 已确认当前已有目标正在执行，因此沿当前 `/goal` 继续，不新建第二个目标。
- 已读取 `Agents.md`、旧 `task_plan.md`、旧 `findings.md`、旧 `progress.md` 和 `docs/product-software-handoff.md` 开头，确认旧接手文档已存在。
- 已确定新文档方向：新增 `docs/software-debug-report.md`，重点不是重复架构介绍，而是整理配置速查、断点位置、观察变量、故障定位流程和测试检查表。
- 恢复计划文件时出现一次 PowerShell 参数错误：`Get-ChildItem -Filter` 不支持多个文件名数组。已记录到计划，后续不重复该失败命令。
- 已扫描主控启动、软任务、公共状态、手柄扫描、电机、泵和压力闭环关键文件。
- 已确认软任务虽然拆成多个 FreeRTOS 线程，但业务回调通过 `AppTaskRuntimeGate()` 串行进入旧业务代码；定位并发问题时要先确认是不是互斥锁串行导致周期等待。
- 已确认泵 `rg` 时出现一次 PowerShell 通配路径错误，实际文件已通过明确路径读取，不影响报告内容。
- 已生成 `docs/software-debug-report.md`，内容覆盖配置速查、全局状态、模块断点、故障现象排查、构建清单和调试记录模板。
- 已完成文档静态检查：UTF-8 可读、无占位符命中、无行尾空格、关键断点函数可检索、`git diff --check` 无错误。

## 2026-06-27 主控原理版调试报告重写
- 已收到反馈：上一版报告没有达到预期，缺少软件设计细节和原理解释。
- 已重新梳理当前源码事实，重点复核启动软任务、公共状态模型、手柄识别、A/B 通道、电机、泵/压力、外控和跨工程协议。
- 已将 `docs/software-debug-report.md` 重写为主报告，叙述顺序改为“为什么这样设计 -> 状态如何流动 -> 代码在哪里 -> 出问题怎么查”。
- 已在报告中补入 Mermaid 数据流和状态流图，覆盖整机五工程关系、输入收敛、手柄识别、控制权仲裁、电机输出、泵压力闭环和外控链路。
- 已在旧 `docs/product-software-handoff.md` 开头增加说明：新版主报告以 `docs/software-debug-report.md` 为准，旧文档保留为历史资料。
- 当前仍限定为文档工作，没有修改业务源码、EIDE 配置或 Keil 工程文件。
- 已完成静态检查：UTF-8 读取正常、占位符/乱码扫描无命中、行尾空格无命中、关键函数和关键验收场景均可检索、`git diff --check` 无错误。

## 2026-06-27 产品手册级调试报告 20 轮重写
- 已收到新的持续目标：报告需要达到产品手册级篇幅和细节，按 20 轮“写作 + 自检”推进，写法保持最终交付口吻。
- 已参考 Zephyr、PX4、Klipper、Marlin 等开源嵌入式/固件文档的组织方式，并把可借鉴点记录到 `findings.md`。
- 已重新扫描当前 `docs/software-debug-report.md` 和 `docs/product-software-handoff.md` 的目录；当前主报告约 1087 行，旧接手文档约 2360 行，主报告需要继续吸收旧文档中的产品级细节。
- 已重新读取主控启动、业务初始化、软任务门控、公共状态结构和手柄扫描源码片段，准备写入报告作为依据。
- 已完成第 1 至第 5 轮扩写：报告新增手册组织原则、工程资料地图、核心结构体源码片段、状态不变量、状态快照模板、启动源码依据、软任务串行门控源码依据、手柄扫描源码依据和手柄识别现场调试流程。
- 第 1 至第 5 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增核心标题均可检索。
- 已完成第 6 至第 8 轮扩写：屏幕输入补充 DWIN 帧解析、VP 区域映射、触控保活源码和调试点；脚踏输入补充启动前置条件、AD 速度换算、松脚去抖和注水泵跟随；手柄按键补充实体键去抖、通道准备、控制权申请和按键队列同步。
- 第 6 至第 8 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增输入链路标题均可检索。
- 已完成第 9 至第 10 轮扩写：控制权章节补充 `s_control_owner`、`ControlArbitration_TryEnter()`、外控进入/释放、按键队列仲裁；电机章节补充 UART1 11 字节帧、速度倍率换算、方向和有刷/无刷选择、驱动回包 Err 映射和停不下来排查链路。
- 第 9 至第 10 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增控制权和电机标题均可检索。
- 已完成第 11 至第 12 轮扩写：泵章节补充 A/B 泵 6 字节输出帧、物理口互换、泵类型速度换算、A/B 周期差和记录模板；压力章节补充软串口 9600 8N1、CS1237 帧校验、设备码白名单、`Cs1237_UpdatePumpMessage()` 状态写入、压力闭环源码依据和压力误停/不恢复定位。
- 第 11 至第 12 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增泵和压力标题均可检索。
- 已完成第 13 至第 14 轮扩写：外控章节补充帧头帧尾、字段解析、命令矩阵、授权长度、2 秒停输出、10 秒释放和动态心跳；UI/蜂鸣章节补充 `UIDPMessage_t`、显示队列去重、泵档位显示换算、蜂鸣队列、定时报警和 LED 状态。
- 第 13 至第 14 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增外控、UI、蜂鸣和 LED 标题均可检索。
- 已完成第 15 至第 16 轮扩写：EEPROM 章节补充 AT24CS32 页结构、Page1 认证输入、Page2/Page3/Page4/Page6/Page8 含义、外控单页读写、运行态和持久化参数区别；跨工程章节补充主控与无刷/有刷驱动板、步进/泵板、压力板、外控上位机的帧格式、源码断点、风险点和协议变更规则。
- 第 15 至第 16 轮自检通过：主报告无学习对象称呼、无乱码/占位符、无行尾空格，新增 EEPROM 和跨工程协议标题均可检索。
- 已完成第 17 至第 20 轮扩写：临时配置章节补充宏、路径、影响范围、验证和回退；故障排查章节补充压力保持停泵、脚踏异常、外控无授权/心跳异常、屏幕触控显示异常；测试章节补充冒烟测试、内容验收场景、专项测试、实机记录模板和串口抓包字段；新增全篇审计和交付检查。
- 第 17 至第 20 轮自检通过：主报告覆盖 20 轮计划要求，后续进入最终静态检查和关键函数检索。
- 根据用户反馈继续增强代码段解释：已扫描 `docs/software-debug-report.md` 中代码块和源码依据章节，补充通用代码段阅读规则、核心结构体字段分组、启动初始化逐段解释、软任务门控分支解释、屏幕保活、脚踏前置检查、实体键准备和启停、电机帧、速度倍率、泵方向、压力帧、压力闭环、外控解析、授权、UI 队列、泵显示和蜂鸣消息等代码段说明。
- 已新增“代码段解释索引”，把关键代码片段对应到业务问题、重点变量、常见误解和实机抓包证据。
- 本轮静态检查通过：占位符/乱码扫描无命中，行尾空格扫描无命中，新增解释标题可检索，`git diff --check` 无错误；只存在 CRLF 提示。
- 根据用户新目标继续补充现场使用内容：已在 `docs/software-debug-report.md` 新增现场定位 SOP、协议帧逐字节判读、报警和保护矩阵、变量字典、配置 cookbook、测试用例表。
- 新增 SOP 覆盖上电、手柄、电机启动/停止、泵 A/B、压力误停、外控授权、旧模块回编译；新增协议帧判读覆盖 UART1 电机下发、UART1 回包、UART5/UART7 泵、CS1237 压力、UART2 外控、UART6 DWIN 屏幕。
- 新增报警矩阵区分真实报警、限时提示和蜂鸣阈值提示；新增变量字典按当前工作、通道记忆、泵压力、外控和任务门控分组；新增 cookbook 和测试用例表用于现场修改和实机验证。
- 本轮静态检查通过：占位符/乱码扫描无命中，行尾空格扫描无命中，新增六类章节标题可检索，`git diff --check` 无错误；只存在 CRLF 提示。

## 2026-07-01 主控工程接手理解
- 已按 `planning-with-files` 恢复旧计划、发现和进度记录；第一次读取因 PowerShell 输出码页显示为乱码，随后显式设置 UTF-8 输出后确认文档中文可读。
- 已扫描顶层目录、`User/Application` 子模块、业务源码文件清单和 `docs` 文档清单。
- 已记录当前工作区未提交改动：业务相关文件为 `Src/main.c`、`sscDrive.c`、`sscFOOT.c`、`Pubinterface.c`、`Pubinterface.h`，构建配置相关文件为 `EIDE/.eide/eide.yml`。
- 已用 `rg` 扫描 EIDE/Keil 构建清单和源码范围，未发现旧状态模块或旧 `Sys*` 全局状态名重新进入构建。
- 已阅读启动入口、业务初始化、软任务门控、公共状态结构、通道记忆装载、插拔处理、压力锁存和脚踏相关未提交差异。

## 2026-07-01 屏幕偶发点不动软件分析
- 已按系统化调试流程，从屏幕触摸输入链路向后追到按键行为队列、控制权仲裁、UI 显示队列和软任务串行门控。
- 已确认 UART6 屏幕收包没有基于 IDLE 的稳定包边界，当前依赖 30ms 任务轮询 DMA 剩余长度和 3 次不变判包。
- 已确认 `ScreenKey_Scan()` 对屏幕帧长度缺少 `dat1[16]` 上限保护，异常长度可通过 `Common_CopyData()` 裸拷贝造成栈越界。
- 已确认普通屏幕键入队非阻塞且忽略失败，`ControlArbitration_ShouldBlockLocalKey()` 也会在 owner 忙时静默丢弃屏幕键。
- 已确认 UI 刷新和屏幕扫描共用 `AppTaskRuntimeGate()` 串行业务门控，大量阻塞式 UART6 刷新可延迟屏幕输入处理。

## 2026-07-10 业务功能代码简化审查
- 已确认本轮只做业务代码只读审查和优化设计，不修改业务源码、不改变任务调度与独立线程栈。
- 已恢复历史计划记录并运行会话追赶检查；历史记录存在，但本轮重新以干净提交 `a39c3b1` 为事实基线。
- 已建立本轮阶段、约束和成功标准，下一步统计当前构建实际包含的业务文件和复杂度，并并行复核关键业务链。
- 已收到用户补充：历史重构尝试烧录后 bug 较多。审查路线已调整为“稳定版行为取证 + 失败重构反查 + 单模块回归闸门”，旧重构记录不再作为可复用设计。
- 已完成第一轮工程规模和构建清单扫描：定位七个主要大型业务文件，确认 EIDE 实际源文件清单与 Keil 核心业务清单，并确认当前独立任务栈和全局串行门控结构保持不动。
- 首轮任务入口检索误用了不存在的 `Src/userparser.c`，已确认真实路径并记录错误，后续改用 `User/Application/Src/userparser.c`。
- 已复核 `Userparser_Init()` 的实际硬件初始化和任务注册顺序，确认后续方案必须原样保持。
- 已提取 AC5 业务 warning 和 map 裁剪结果，识别出一批不进入最终镜像的旧函数，可作为低风险重构流程试点；大型状态机暂不直接动手。
- 已统计公共状态引用分布并按函数群梳理 `Pubinterface.c` 与 `external_comm_task.c` 的职责；确认状态所有权和跨模块直接写入是主要架构问题。
- include 依赖检索出现一次 PowerShell 引号错误，状态赋值初筛也误包含 `==` 条件；两项均已记录，相关原始输出不作为最终结论。
- 已用排除比较运算的模式重新统计公共状态直接赋值位置，并确认外控与脚踏各自都有大量跨模块写入；后续设计必须先收敛动作入口，再考虑移动状态定义。
- 已重新列出 17 个实际启动周期入口和周期，确认 `PedalRecvTask_Init()` 只是未接入启动链的旧入口。
- 已结合 map 审查旧模块：区分了整段被裁剪的兼容代码、仍保留上电安全职责的 `motor.c`，以及仍形成第二泵输出入口的 `pump.c`，避免把“文件旧”误判为“整文件可删”。
- 已检查业务目录和函数注释覆盖，确认第一轮不应移动 `Beep` 下的历史文件，也不应脱离业务改动批量重写注释。
- 已抽查多个历史风险点，确认手柄空指针和 RFID 尝试计数注释不一致；脚踏两处 A/B 差异暂列疑点。
- 已推翻旧记录中的“12 字节电机回包必然漏解析”结论，后续所有风险均按当前源码重新验证，不继承旧报告判断。
- 已复核屏幕收包边界和 A/B 映射链：确认屏幕长度缺少 16 字节上限保护，并固定记录稳定版的泵驱动口、压力口、显示区域和 25ms 周期不变量。
- 对 stash 做差异检索时 PowerShell 误解析了 `stash@{0}`，已记录并改用 `refs/stash` 方案；实验提交与稳定版的大范围差异已确认，禁止作为整体复用。
- 已确认 `Pubinterface.h` 的暴露面、历史测试脚本缺失、长文档压力映射过期；后续以源码和 map 为真值，并把文档同步放在每个稳定模块阶段末尾。
- 已完成历史失败模式反查：确认错误基线、跨模块同时改和屏幕单层镜像是已证实问题；当前独立线程与全局互斥不动。
- 已补齐电机、按键、蜂鸣、LED、看门狗专项审查，识别出多项隐式时序和独立安全缺陷；这些缺陷不会混入机械重构阶段。
- 已完成公共状态写入者、异步插拔过渡态、报警和泵字段所有权审查，确认新方案应先收敛业务动作入口，再缩小公共头文件。
- 已形成三种路线比较并选定“行为锁定的纵向小步重构”为推荐方案；计划要求每个烧录版本只处理一个模块，历史实验工作区和镜像宏补丁明确废弃。
- 本轮业务源码保持只读，下一步只向用户提交审查结论和推荐设计，得到确认后才编写实施计划或修改代码。
- 已完成交付前验证：AC5/EIDE 全量构建 0 error、22 个基线 warning，RO/RW/ROM 与基线一致；`git diff` 未包含任何 `.c/.h` 或工程清单修改。
- 已删除本次构建生成的 EIDE `.lock`，未生成临时检查脚本或额外测试文件。

## 2026-07-12 脚踏业务模块设计

- 已将通过实机验证的泵公共内核、排空高亮和双压力串口修复提交为 `c503979`，提交后工作区干净。
- 已开始只读审查脚踏模块，确认当前启动入口、队列长度、25ms 周期以及未启用的旧接收任务边界。
- 当前尚未修改脚踏生产源码；下一步继续拆解三类踏板分支并形成可供确认的模块级设计。
- 已完成 `FootControlTask()` 全分支和 map 调用关系审查，确认主要重复集中在启动门禁、运行状态写入、注水泵联动、释放复位及双踏板左右镜像流程。
- 已识别解析任务和行为任务属于两个独立风险面，准备先请用户确认本轮是否只重构行为任务。
- 用户已确认本轮只重构 `FootControlTask()` 行为逻辑，UART4 解析保持不动。
- 已按确认方案生成模块级设计规格，等待完成静态自检和设计节点提交。

## 2026-07-10 业务重构设计固化
- 用户已确认采用“行为锁定的纵向小步重构”路线。
- 用户明确要求每次优化先完成本机测试和实机确认，确认无问题后才提交 Git。
- 已开始把确认内容写入正式设计规格，本阶段不修改业务源码。
- 已完成规格自检：无 TBD/TODO/待定占位，任务模型、A/B 映射、单模块边界和提交闸门前后一致。
- 已验证设计规格为有效 UTF-8，共 372 行；`git diff --check`无内容错误，当前没有任何 `.c/.h`或工程清单差异。

## 2026-07-14 当前工程行为保持优化审查
- 已确认当前提交为 `040cd17`，开始审查时工作区干净。
- 已恢复历史计划、发现和进度记录，并运行 planning-with-files 会话追赶检查。
- 已确认本轮只做当前源码只读审查，不修改业务功能、任务模型、协议、A/B 映射或五个核心业务数据结构。
- 已从当前构建清单和源码重新统计大型文件、`Pubinterface.h` 依赖面及五个核心对象直接写入者，旧审查数据不再作为当前结论。
- 已确认手柄、脚踏、泵和屏幕解析等已完成实机验证的模块不作为本轮首选重构对象。
- 正在补齐外控、公共接口、UI/电机杂项三组并行审查与 map 死符号证据。
- 已用当前 map 区分“无调用但仍占镜像数据”的旧全局变量和“完整被链接器裁掉”的兼容模块，识别出两个可安全优先清理的空适配层。
- 已完成 Pubinterface、外控、UI/电机/RFID 三组只读审查，形成低风险清理、中风险原文件重构、独立行为缺陷和禁止触碰时序四类结论。
- 已确认未启动的 RFID 旧任务对象独占4240B RW，是下一阶段最优先的零运行路径清理项。
- 已重新执行 EIDE/AC5 全量构建：127个C、1个汇编，0 error、0 warning；RO 106040B、RW 130664B、ROM 106524B。
- 交付前只保留三份规划记录差异，没有业务 `.c/.h`、构建清单或生成产物差异。
- 已删除全量构建生成的 EIDE `.lock`；最终空文件、未跟踪文件、源码差异和 `git diff --check` 均无异常。

## 2026-07-14 手柄物理 A/B 接口交换
- 用户确认实施统一的 `HANDLE_PHYSICAL_AB_SWAP_ENABLE` 配置，本轮直接修改并验证，不另写长篇实施计划。
- 已完成短接检测、I2C EEPROM、实体键、RFID、外控 EEPROM 和电机驱动物理通道的当前绑定核对。
- 已确认工作区原有文档整理改动属于用户现有内容；本轮将保留并只修改与手柄物理映射直接相关的源码及唯一软件说明手册。
- 已建立一次性结构校验并确认 RED：当前工程缺少统一交换宏，handlescan、外控、RFID和电机仍各自硬编码物理 A/B。
- 已新增统一映射函数和资源宏，结构校验转为 GREEN；逻辑绑定表、A后B扫描顺序和公共状态对象未修改。
- 默认交换 `1U` EIDE/AC5 全量构建通过：127个C、1个汇编，0 error、0 warning；RO 106168B、RW 130664B、ROM 106652B。
- 临时关闭交换 `0U` EIDE/AC5 全量构建通过：127个C、1个汇编，0 error、0 warning；RO 106076B、RW 130664B、ROM 106560B。
- 首次批量更新软件手册因维护记录原文与预期上下文不同而未应用，已改为小块精确补丁，未影响源码或文档已有内容。
- 已把全部触碰源码恢复为UTF-8无BOM、纯CRLF，严格UTF-8读取和差异空白检查通过。
- 恢复默认交换 `1U` 后完成最终EIDE/AC5全量构建：0 error、0 warning；RO 106168B、RW 130664B、ROM 106652B。
- 最终map确认手柄扫描、实体键、RFID和电机四个独立任务对象均保留，每个任务对象仍为4240B；未改变任务模型。
- 一次性结构校验脚本和EIDE `.lock` 已删除，当前等待实机验证A/B插拔、运行键、RFID、电机启停与定位。
- 用户要求提交当前工程；提交前已重新核对最终宏为手柄物理交换0、R200-K8交换0、单串口模式、美国频段和RFID功率10dBm，并再次执行EIDE全量构建。

## 2026-07-15 外控EEPROM安全边界与导航批量读写
- 已确认用户要求：业务EEPROM协议层只读；导航EEPROM开放批量读写。
- 已读取主控与上位机真实代码，确认现有批量只是80ms间隔的单页循环，主控0x06/0x09仍明确不支持。
- 已启动主控协议和上位机批量队列两项并行只读审查；生产代码尚未修改。
- 已完成主控修改：0x07固定拒绝，0x09导航批量读，0x0C导航批量写；批量锁定I2C总线并按10ms周期逐页推进。
- 已完成上位机修改：业务页保持只读，Page7/Page10保留页跳过；导航批量按钮只发一条范围命令并等待逐页结果和最终ACK。
- 已同步`docs/软件说明手册.md`与上位机`说明文档.txt`，删除旧的“外控可写Page4/Page6”和“0x09禁用”说明。
- 静态安全回归、上位机5项现有回归和模块脚本语法检查均通过；一次性校验文件已删除。
- EIDE/AC5全量构建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 106604B、RW 130704B、ROM 107088B。
- EIDE生成的`.lock`已删除；当前未提交Git，等待主控与手柄EEPROM实机联调。
- 独立复核发现并修复批量状态机边界：主控增加运动停止门禁和0xFF启动失败对象；上位机增加单页/批量互斥、严格页序、清空门禁及手工解析隔离。
- 上位机按钮明确为“当前数据批量写导航页”，0x0C仍把同一份30字节数据写入所选范围；批量期间禁止切换逻辑A/B，避免界面通道与锁定总线产生理解歧义。
- 最终上位机模块脚本语法、5项既有回归及新增静态安全回归通过；一次性Python/JS校验文件已删除。
- 最终EIDE/AC5全量重建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 106728B、RW 130704B、ROM 107212B。

## 2026-07-15 导航批量读取实机问题修复
- 已读取两张实机截图：批量读取在实际 Page12 返回设备失败0x04；Web Serial同时出现可恢复 `Framing error`，顶部显示“串口读异常，保持连接”。
- 已确认当前主控批量读遇任一坏页立即终止，且10ms页上传可能与100ms心跳同周期发送；当前进入针对性修复阶段，尚未提交Git。
- 已读取异常压力截图并确认上位机 `applyTelemetry()` 缺少 `decoded.valid` 门禁；CRC/帧尾失败心跳仍会写入压力和泵速曲线，已纳入本轮最小修复范围。
- 已完成主控修改：导航批量页节拍为30ms，读坏页继续、写坏页停止，最终ACK单独发送，到期心跳避开已发送批量帧的周期。
- 已完成上位机修改：无效心跳不再刷新压力/泵速/导出日志，可恢复读异常只记日志，导航坏页结算后继续等待整批完成。
- 新增稳定性静态回归由失败转为通过；上位机模块脚本语法和5项既有回归全部通过，一次性Python/JS校验脚本已删除。
- 最终EIDE/AC5全量重建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 106768B、RW 130704B、ROM 107252B。
- 提交前确认当前硬件配置为 `HANDLE_PHYSICAL_AB_SWAP_ENABLE=1U`、`RFID_USE_DUAL_UART_MODE=1U`，逻辑A RFID固定UART3、逻辑B RFID固定UART9；使用该最终配置再次全量构建通过，RO 107052B、RW 130848B、ROM 107536B。

## 2026-07-15 EEPROM批量读写进度显示
- 已确认批量期间为避免117次整表重绘，结果区会延迟到批量结束才统一刷新；现有界面只有时间统计，没有运行中页进度，因此容易被误认为卡死。
- 已锁定最小方案：新增一个小型进度组件，直接复用`eepromBatchOperation`的逐页结算状态和最终ACK状态，不增加模拟进度或新的协议字段。
- 已完成进度组件：显示已处理页、总页数、百分比、当前状态和失败页数；批量写失败页也计入终态，达到100%后继续等待最终ACK。
- 已完成上位机模块脚本语法、进度静态校验和5项既有回归，全部通过；本轮只改上位机界面和说明，未修改主控协议或固件源码。

## 2026-07-15 EEPROM批量活动状态替代进度条
- 用户实机确认确定型进度条视觉上为0后瞬间满格；逐页回包在浏览器同一处理批次内快速结算，中间DOM宽度状态没有稳定绘制机会。
- 已决定改为不定进度活动状态卡：旋转工作指示持续表达“程序仍在运行”，同时保留真实页计数、最近页面、失败页和实时耗时。
- 已删除旧`progressbar`、百分比计算、填充宽度和扫描动画，改为合成层旋转指示；完成、部分失败和中止分别显示勾、警告和错误终态。
- 活动卡每250ms只刷新本机耗时，页数和最近页面仍由真实回包更新；模块脚本语法、活动组件校验和5项既有回归全部通过。
- 已同步上位机说明和唯一软件说明手册；本轮未修改主控协议或固件源码，一次性校验文件已删除。
## 2026-07-15 上位机四工作台界面改造
- 用户已审核并确认采用固定状态栏加四个顶层工作台方案，开始直接实施。
- 已读取前端设计、界面细节、精简修改和文件化规划规则；工业控制台视觉方向、单HTML和无重载切换边界已锁定。
- 已恢复上一轮上下文并核对当前HTML结构；本轮尚未修改主控协议或固件源码。
- 已完成实时监控、设备控制、EEPROM维护和通信诊断四个工作台；顶部串口、外控、通道、运行状态和急停保持全局可见。
- 已把EEPROM后台任务同步为工作台状态徽标，继续使用活动卡和逐页文字摘要，不恢复百分比进度条。
- 已完成隐藏Canvas重绘、ARIA选中状态、方向键/Home/End键导航和窄屏纵向降级。
- 临时静态检查通过：1个模块脚本、156个唯一DOM ID、4个工作台；现有5项上位机回归全部通过。
- 本机Edge在1920×1080和1366×768完成四工作台视觉及切换验证，四个桌面工作台均无整页滚动；1024宽度按设计降级为纵向布局。
- 已同步唯一软件说明手册和上位机说明文档；未修改本轮之外的主控协议或固件业务代码。

## 2026-07-15 四工作台分栏调节与导出入口恢复
- 用户反馈四工作台左右栏宽度需要可调，并要求恢复JSONL、通用CSV、压力CSV、手柄CSV和电流CSV等独立导出按钮。
- 已恢复上一轮四工作台上下文；本轮边界固定为仅修改上位机布局与交互，不修改主控协议或固件业务代码。
- 已实现四工作台独立分栏比例、拖动、方向键微调、Home/双击复位和localStorage刷新恢复；窄屏自动隐藏分隔条。
- 已移除单一折叠导出菜单，恢复JSONL、通用CSV、压力CSV、手柄CSV和电流CSV五个独立可见按钮，原ID和事件函数保持不变。
- Edge在1366×768实测各工作台比例互不影响、刷新恢复和双击复位正常，五类按钮均触发对应文件下载；1920×1080四工作台均无整页滚动。
- 模块脚本、157个唯一DOM ID、3条分隔线、5个导出按钮静态检查及5项既有上位机回归全部通过。
- 已同步上位机说明和唯一软件说明手册；未修改主控协议或固件业务代码。
- 临时浏览器、本地HTTP服务和一次性校验脚本已关闭或删除；上位机目录无锁文件、临时文件、空文件或UTF-8替换字符。
## 2026-07-15 上位机工业级视觉体系升级
- 用户反馈现有四工作台虽然解决了长页面问题，但整体质感仍偏平，在线/离线、运行/停止和数据类别颜色区分不够明显。
- 已锁定本轮仅修改外部通信上位机视觉和必要语义状态属性，不改变主控协议、固件、EEPROM安全边界、控件ID、导出功能和分栏交互。
- 已读取前端设计、界面细节、精简修改、文件化进度和浏览器实测规则；下一步先用官方成熟工业产品资料校准视觉原则并截取当前基线。
- 已参考Grafana、Siemens WinCC Unified和Ignition Perspective官方资料，建立业务语义色和表面层级；三套背景主题继续可切换。
- 已把顶部压缩为54px单行四区，设备控制命令区改为紧凑双列，右侧加入趋势图填充原大块空白；1366×768和1920×1080均无整页或控制命令区上下滚动。
- 已为通道、运行和数据卡补充最小语义属性，Canvas曲线与卡片共用A/B、速度和电流主题色，未改变实时数据源或解析路径。
- Edge验证四工作台、三套主题、三条分隔线和五类导出入口；模块脚本无错误、DOM ID无重复，5项既有上位机回归全部通过。
- 已同步上位机说明文档与唯一软件说明手册；临时浏览器检查脚本、本地HTTP服务和日志均已清理，本轮不提交Git。

## 2026-07-15 前三工作台空间再平衡
- 用户反馈第1至第3工作台仍有较多空白；截图确认实时监控左栏下半屏空置，而右侧图表出现内部滚动。
- 已锁定仅调整上位机前三工作台桌面布局，不修改协议、控件ID、串口状态、EEPROM安全边界、导出或主控固件。
- 已完成本机原图视觉检查并建立本轮阶段计划；下一步测量前三页真实DOM尺寸和滚动边界。
- 已启动本地HTTP页面并用Edge测量1920×920和1366×768：第1页确认左右高度严重失衡，第2页现有右侧布局合理，第3页需要弹性数据区吸收屏高差。
- 已确定最小布局方向：第1页左侧状态下放运行趋势、右侧专注压力；第2页只优化大屏命令区密度；第3页右侧改为固定摘要与版本、弹性原始数据/解释区域。
- 已完成前三页布局重排：实时监控左右负载平衡、设备控制摘要单行并扩大趋势、EEPROM数据查看区弹性填充。
- 已按用户反馈移除设备/刀具/压力卡左侧半括号式粗线，改用完整圆角细描边和右上状态点。
- Edge在1920×920及1366×768完成最终验收：三页无整页滚动、状态与图表无裁切；4个工作台、3条分隔线、5类导出和所有控件ID保持完整。
- 5项既有上位机回归全部通过；外控协议、Web Serial状态、EEPROM安全边界和主控固件均未修改。

## 2026-07-16 移除重复自动协议解析窗口
- 已按截图锁定范围：删除右侧自动协议解析面板，保留左侧通信日志、上方手动协议解析和下方配置导入。
- 已确认自动面板同时承接 TX/RX 自动刷新、日志点击和手动粘贴三类结果；下一步将只删除自动刷新，把两类主动查看结果迁入手动解析卡片。
- 已删除独立自动协议解析面板和 TX/RX 自动展开调用；实时收发只写通信日志，主动解析结果收进手动解析卡片。
- Edge在1366×768和1920×920验证：自动面板数量0、页面无整页滚动、配置区分别保留383px和535px高度；手动解析展开后页面仍不滚动。
- 五类导出、唯一DOM ID、模块脚本语法、手动解析清空行为和5项既有上位机回归全部通过，浏览器控制台无错误。
- 已同步上位机说明文档和唯一软件说明手册；临时脚本、截图及本地HTTP服务均已清理，本轮未修改主控固件。

## 2026-07-16 上位机掉线统计与顶部可读性
- 已检查两张用户截图：顶部状态文字需要放大；掉线统计要求完全由上位机根据收到的状态变化生成，不依赖主控新增事件。
- 已锁定安全边界：串口整体断开和心跳超时不展开为六个设备掉线，首帧离线不计数，只有稳定在线到稳定离线的边沿才记录。
- 已完成上位机A/B手柄、泵和RFID识别结果掉线记录；连续3个有效心跳确认边沿，串口断开和心跳超时只重置观察器。
- 用户要求提高RFID丢包率准确性后，新增`RFID_LINK_STATS_ENABLE`：主控按实际EPC命令累计请求、有效应答、丢失和异常帧，上位机只展示A6心跳快照，不再本地估算。
- 统计开启与关闭两种配置均通过EIDE/AC5全量构建，127个C、1个汇编，0 error、0 warning；关闭时RW减少32B并恢复原心跳格式。
- A6样例解析通过：A通道10/100显示10%，B通道2/50显示4%，异常帧分别为3和1；无浏览器脚本错误。
- 已修正实时串口接收入口错误传入`recordDeviceStatistics:false`的问题；浏览器模拟B通道133/133后右侧立即显示0/133和0.00%，手工解析200/100/100测试帧不会覆盖现场统计。
- 顶部四项状态改为16px/40px高；1920×1080保持单行，1366×768使用两行分区，四项状态均完整可见且页面无整页滚动。
- 已同步上位机说明文档和唯一软件说明手册；最终交付构建为RO 107452B、RW 130736B、ROM 107936B，HEX SHA256为`51ED8FE8BFFA7387C51483BE4521E2AC24D8131D662119C395DF42B9D9E18DC0`。
- 临时校验脚本、本地HTTP服务、EIDE `.lock`和空依赖文件已清理；当前未提交Git，等待实机观察A/B射频丢包率。

## 2026-07-16 主控RFID超时掉线统计与诊断布局调整
- 用户要求移除通信诊断页配置导入，把右栏空间用于设备掉线统计并放大统计文字；同时要求主控依据现有RFID超时判断累计掉线并触发蜂鸣。
- 已锁定最小实现方向：复用现有请求超时结算，不新增第二套计时器；掉线统计继续通过A6扩展实时上报，上位机不自行推断主控超时事件。
- 已定位主控统计结构、A6追加入口和请求自然耗尽分支；正在核对剩余尝试次数实际递减位置及现有蜂鸣接口，确认超时边界真实可达后再修改。
- 已确认真实掉线边沿位于handlescan在线监测：200ms一次、连续10次未确认后清刀具并单次蜂鸣；统计复用该边沿，没有新增第二套计时器或重复蜂鸣。
- 已扩展A6版本2，按A/B上报在线监测完成数和确认掉线数；上位机兼容A6版本1并实时计算确认掉线率。
- 已移除通信诊断页配置导入，右栏改为紧凑手动解析工具区和设备健康统计主区，五类统计在1366×768首屏完整显示。
- 已按Apple/Emil规范统一系统字体、半透明材质、圆角、阴影、状态层级和按钮按压反馈；实时数据与高频导航不增加装饰动画。
- 主控统计开关1/0均通过EIDE/AC5全量构建；最终1U版本为127个C、1个汇编，0 error、0 warning，RO 107676B、RW 130744B、ROM 108160B。
- 上位机A6版本1/2解析、实时卡片刷新、配置导入移除和1366×768四工作台Edge渲染检查通过。

## 2026-07-16 设备控制页视觉优化
- 已读取用户截图和Apple/Emil/前端设计及动画审查规则，范围固定为上位机设备控制页视觉与展示语义，不修改主控协议或业务控制逻辑。
- 已建立按钮层级、状态图标可读性、桌面无整页滚动和交互回归成功标准；下一步核对真实DOM与状态属性后实施。
- 已完成设备控制页专用CSS：A/B切换、方向、刀具、启停和开口定位使用文字加紧凑符号块，启动/停止分别使用绿/红语义色，A/B继续使用蓝/紫通道色。
- 已放大控制页手柄/泵状态卡：1920下设备卡82px、图标58×46px、状态点10px，并提高设备名称、离线/在线文字和当前工作标签字号。
- 已在1366×768验证页面无整页滚动，左侧低高度局部滚动可访问全部安全控制按钮；1920×920下控制区完整同屏且按钮不再随高度拉伸。
- 已在极昼主题核对启停、A/B和刀具语义色真实生效，浏览器运行日志无error或warning；控件ID、data-action、协议构造和控制事件均未修改。
- 已同步上位机说明文档和唯一软件说明手册；进入最终编码、差异、临时文件与浏览器视口清理检查。
- 最终收口时识别并修复Node REPL文件桥接附加的4096字节异常前缀；6个文本文件均按原始长度和SHA256无损恢复，严格UTF-8、CRLF、浏览器加载与五项现有上位机回归重新通过。
- 已确认剩余尝试次数当前不会递减，任务实际以100ms持续轮询；因此不能把不可达的“自然耗尽”当作掉线判断，下一步改查handlescan对RFID存在序号的既有超时和蜂鸣边界。
- 已确认真实掉线边界位于`handlescan.c`：每200ms发起一次在线监测，连续10轮未由presence序号确认后清刀具并单响一次，持续离线不会重复执行。
- 已锁定主控掉线率定义：确认掉线次数 / 已完成在线监测次数；监测成功和未响应都进入分母，只有在线到离线确认边沿进入分子。
- 已确定A6扩展升级为版本2：A/B固定顺序，每通道20字节，在保留请求/有效/丢失/异常统计的同时增加监测完成数和确认掉线数；上位机继续兼容A6版本1。

## 2026-07-16 上位机四工作台切换流畅度优化
- 已读取Apple/Emil、精简修改和文件化规划规则；成功标准固定为即时标签反馈、减少隐藏页重绘、短时可中断过渡且不改变任何协议或运行状态。
- 已恢复现有计划、发现和进度记录并执行session catchup；当前进入切页函数、CSS显隐和Canvas刷新链路核对阶段。
- 已完成四工作台切页性能修复：页签与骨架优先显示，隐藏页日志/统计只置脏，Canvas仅在可见页合帧重绘且像素尺寸未变化时复用缓冲区。
- 已将日志列表改为一次性HTML构建和父容器事件委托，并补齐当前可见180条的有界对象索引，保证手动翻阅与长期运行均可点击解析。
- 正式上位机文件已写回；5项既有上位机回归和内联模块语法检查通过。
- 无头Edge完成原版/优化版各80次四页切换：0运行异常、0长任务；隐藏50条日志耗时约379.8ms降为0ms，第一帧P95约33.1ms降为31.9ms。

## 2026-07-17 外控切换手柄通道成功提示音
- 已确认外控 A/B 通道切换本身成功，缺少蜂鸣的原因是协议成功分支没有投递任何按键音消息。
- 已确认现有普通按键音会清除蜂鸣任务内部报警状态，因此本轮采用独立空闲提示音，避免通道提示覆盖报警。
- 已锁定修改范围为 `sscBEEP.h/.c`、`external_comm_task.c` 和软件说明手册；不修改上位机协议、ACK、屏幕刷新或其它切换项。
- 已新增报警空闲提示音消息，并在外控 A→B/B→A 成功边沿投递 1 个 100ms 周期；重复选择、初次选中及失败请求均不响。
- 已同步软件说明手册；静态检查确认提示音不会清除持续报警或限时报警倒计时。
- EIDE/AC5全量构建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 107824B、RW 130744B、ROM 108308B。

## 2026-07-17 外控切换通道时脚踏图标短暂变黄核实
- 用户实机确认外控通道切换蜂鸣正常，但插着脚踏切换 A/B 时，实体屏幕脚踏图标会短暂由白色变黄。
- 本轮范围固定为只读追踪通道记忆、外控 owner、脚踏工作模式和屏幕刷新顺序，不修改任何业务源码。
- 已确认黄色瞬态由 `Pubinterface_LoadChannelMemory()` 的脚踏优先分支写入 `JTWORK`，随后屏幕快照按该字段高亮脚踏。
- 已确认外控 owner 全程未释放，脚踏任务在 owner 门禁处只更新连接状态后返回，不会启动电机、泵或切换通道。
- 已确认上位机每200ms重复申请保活，下一帧会把模式恢复为 `TOUCHWORK` 并重绘白色脚踏图标。
- 用户确认按推荐方案修复；`Pubinterface_LoadChannelMemory()` 现已优先保持外控 `TOUCHWORK`，脚踏在线分支只在非外控状态继续生效，且不改目标通道本地控制方式记忆。
- EIDE/AC5全量构建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 107840B、RW 130744B、ROM 108324B。


## 2026-07-20 RFID重试、泵队列与压力数据校验修复
- 已恢复文件化计划并确认工作区既有手册、DOCX和构建日志差异属于用户/历史修改，本轮不覆盖。
- 已确认RFID递减语句被注释、A/B泵入队前更新去重缓存、CS1237仅做结构与CRC校验三项根因。
- 已从压力板协议和源码确认RawCs1237为24位符号扩展，ThresholdG配置上限为50000g；压力校验复用这些现有边界而不猜测新的产品阈值。
- 已完成三项局部代码修改，下一步执行结构回归、编码检查和EIDE/AC5全量构建。
- EIDE/AC5全量构建通过：127个C文件、1个汇编文件，0 error、0 warning；RO 108292B、RW 130752B、ROM 108776B；最终HEX SHA256为7594845C4628D1D8E376626F4D497749B037950B329C2B1EE09FBB0D3C956560。
- RFID、A/B泵和压力边界GREEN校验通过；EIDE锁文件与一次性补丁已清理，未修改压力板工程、任务周期和公共消息结构。

## 2026-07-24 固定方向手柄与MXYTP机械往复适配
- 已恢复既有规划记录并锁定当前未提交文档边界；开始核对七类手柄的型号、EEPROM默认方向、屏幕显隐和驱动实际方向。
## 2026-07-20 剩余可靠性与维护性改进方案
- 已以`667bf66`为基线复核手册风险表、活动源码、Map、FreeRTOS调度门和关键UART状态机。
- 已剔除刚完成的三项修复，并把剩余工作分为零行为清理、安全故障闭环、通信健壮性、运行诊断和受控维护性整理五组。
- 已明确正常业务路径保持不变；电机失联、IWDG、报警优先级和通信协议完整性属于故障语义变更，必须独立实施并做实机/对端联调。
- 本轮未修改业务源码、配置宏或构建清单，也未生成新固件；只更新文件化审查记录。
## 2026-07-21 Page3两位小数适配
- 已恢复既有规划记录、MEMORY中EEPROM写入工程背景和当前三工程状态；已启动主控、写入工具、外部上位机及同族实现四路只读审查。
- 已确认主控Page3减速比/增速比分别是单字节整数，运行链再换成x10；外部上位机现有Page3两个倍率标签顺序与主控相反。当前进入字节布局和兼容策略确认阶段，尚未修改业务源码。
- 已核对EEPROM写入工具：codec、GUI、CSV和读回链均把两个倍率限制为整数单字节，相关单元测试已有明确覆盖；下一步核实Page3 `[10..14]`真实占用和旧数据兼容标记。
- 已确认主控所有倍率运算集中在电机速度换算，32位字段的每个16位半区可用格式标志区分Page3 x100和现有RFID x10，无需修改公共状态结构。
- 已锁定兼容格式候选GR02，并确认无需改EEPROM写入板UART协议：它仍透明传输30字节；需要修改的是PC codec/表单/CSV/读回和主控Page3解析及电机倍率换算。
- 已确认GR02+两个百分位只需把主控Page3读取长度增至17；内部采用16位半区标志可保持A5块长、公共结构体和RFID协议不变。
- 阶段1/2完成：已确定GR02兼容布局、0..255.99且最多两位小数的输入规则、主控x100内部单位和A5 v1/v2兼容策略，开始修改三工程。
- 已定位主控手册和写入工具打包入口；三路代码代理正在并行实施，主线程准备文档同步、构建与最终跨工程审查。
- 已完成三端实现：主控解析`GR02`并统一x100运行倍率；EEPROM写入工具支持两位小数、读回、CSV/JSON及认证重算；外部上位机修正Page3方向并兼容A5 v1/x10与v2/x100。
- 主控EIDE/AC5全量构建通过：127个C、1个汇编，0 error/0 warning，RO 108952B、RW 130760B、ROM 109436B；HEX SHA256为`4E1AA65D51BDE92D3C1B91D791E282EB04476ED6C033CFCBF63364660CDE1242`。
- EEPROM写入工具71/71单元测试通过，固件构建Code 13544B、RO-data 544B、RW-data 20B、ZI-data 2948B；HEX SHA256为`AA6DAEF4AA16022B2850761C4A7F358F18C6945ADB3F4BBF7B741471B92D8392`，EXE冒烟通过且SHA256为`C7088F6419013B009A5DF517F705A29ABD0624034B26E23680E0903A06136DE3`。
- 外部上位机正式目录的脚本语法和心跳/Page3样例回归通过；业务EEPROM只读边界未改变。
- 已删除一次性补丁、测试残留、构建锁和主控临时构建日志；主控和写入工程差异检查通过，构建目录无空文件，最终独立审查无阻塞项。

## 2026-07-23 UART2简易外控协议并行支持
- 已恢复现有规划记录和提交基线`d80de4d`；工作区原有异常协议DOCX及两份未跟踪DOCX继续保留且不触碰。
- 用户明确要求新协议单独文件；已锁定为独立`external_comm_simple_protocol.c/.h`，原协议文件只保留FIFO候选交接和共享安全服务的窄接口。
- 已确认新协议不应独立扫描DMA原始包，必须在共享FIFO中按最早帧头分流，避免旧协议载荷误命中简易帧。
- 已确认调速、泵档位、通道切换和异常提醒均有现有业务规则可复用。
- 已新增独立`external_comm_simple_protocol.c/.h`，12个功能码、会话状态、固定帧校验和编译关闭空实现均留在该模块；原任务只增加帧头分流和三个窄复用接口。
- 已同步EIDE配置、EIDE实际参数、根构建参数和Keil工程清单；唯一软件说明手册同步命令表、开关、协议边界、测试矩阵和文件索引。
- 半包、粘包、坏尾、旧协议载荷误命中、12个功能码及清提醒安全边界静态回归通过。
- 宏1U和0U均按EIDE/AC5全量构建128个C、1个汇编，0 error、0 warning；最终恢复1U并重建，RO 109848B、RW 130760B、ROM 110332B。
- 最终HEX SHA256为`3D533D36B027C42D0DD041CFCB817FA4B7B7A8CB74C5C6EF57E8E7C0A4390651`；构建锁和一次性校验文件已清理，用户现有DOCX差异未触碰。

### 固定方向手柄适配进展
- 已完成型号、Page4、显示、屏幕输入、实体键/HMI、外控和 UART1 驱动方向链核对。
- 已实现七类固定方向门禁：当前方向高亮、其它方向显示灰色禁用态；MXYTP保持界面往复/驱动正转分离。
- 已通过 `git diff --check`、调用点静态复核和 EIDE/AC5 全量重建。
- 已修正RFID标签电流单位：EPC byte11按0.1A解释并乘10转换为主控/驱动0.01A，标签100对应10.00A。
- 最终构建为128个C、1个汇编，0 error、0 warning；RO 110240B、RW 130760B、ROM 110724B。
- 最终HEX SHA256为`5807FD84A6E9753EFA8E4CE925D79F3A6044767008A328C3BCAED4489ED73001`。
- 本轮EIDE `.lock`和空构建文件已清理；未跟踪的DOCX副本与生产报告保持原样，不纳入主控提交。

### 本轮工具异常
- 两次内层 PowerShell 读取命令因外层变量/引号展开失败，均未修改工程；后续改用 `rg` 定位。
- 首版两个临时 Git 补丁因 hunk 计数或上下文不准确被 `git apply --check` 拒绝，未产生部分源码修改；修正后均先校验再应用。
